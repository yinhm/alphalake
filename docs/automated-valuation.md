# A 股自动化估值

目标是所有已发现的 A 股都有可解释的处理结论：完成条件估值、缺数据、缺政策或模型不适用。字段齐全不等于估值完成，不能为提高成功率将未知值补零。

**最新验收**：[全市场结果及完整运行命令](a-share-automation-acceptance-20260910.md)。主库已更新：本地5,569家全部有结论，5,304家核心输入齐全、2,461家条件股权估值，零执行异常。下文为各阶段记录，“副本/待执行”等描述保留当时状态，以最新验收为当前交付口径。

## 财务就绪度扫描

```bash
alphalake valuation-readiness ./alphalake.duckdb \
  --period 2026-06-30 --as-of 2026-09-09T18:00:00Z > readiness.json
```

只读查询同一事务中的本地证券主数据和标准 ASOF/TTM 窗口，不读取 PDF 直算，不修改事实。分母包括本地已知、未退市的沪深北人民币股票，无标准事实或无唯一 TDX 身份也保留。ETF、指数、外币股票排除。证券身份有效日取信息截止的中国日期。

输出逐公司最新可用财报期、全部已审核字段窗口及源事实 ID、15 项核心字段缺项和汇总计数。核心检查覆盖收入、营业利润及利息/非经营损益、股本、现金、借款/债券/租赁/到期负债和少数权益；它是筛查契约，不是通用 DCF 的全部输入契约。未披露零仍缺失，核心齐全仍明确要求模型政策与市场输入评估。

`universe_scope` 明示：这是本地已发现集合，尚未与交易所完整名单对账；主数据名称/状态不是完整历史快照，不能据此声称无幸存者偏差的历史全市场覆盖。指定期间缺项不会静默退回上一份年报。

## 推进顺序

1. 建立真实证券分母、标准财务覆盖及缺项排行。
2. 将既有标准导出/政策校验/共享引擎接入批量任务，按公司隔离失败，保留成功版本及失败原因。
3. 增加普通非金融企业的通用、版本化假设规则和适用门槛；公司特有政策继续独立，金融企业不套普通 FCFF。
4. 根据实际阻塞补数据、行业映射和市场输入，做跨公司真实源验收，再扩增量调度。

就绪度扫描、按公司隔离的批量估值、通用政策及行业路由、公告补采和参考源刷新入口均已实现。全市场数据、行业政策和原文审核的覆盖仍分别计量；下文实测按运行时点记录，不将双公司验收库的完整率当作全A股完整率。

## 独立刷新证券名单

```bash
alphalake sync-instruments ./alphalake.duckdb
```

复用既有分区主数据刷新与三节点重试，不下载日线。健康分区独立发布，分区失败使运行标为 partial 并返回非零退出码；失败诊断保留，不能把部分名单当完整市场。已有历史名单的保留和两次缺失确认规则不变。

## 批量运行已有审核政策

在 `valuation/backend` 下执行（使用已安装后端依赖的 Python）：

```bash
python -m tools.batch_valuate_alphalake /absolute/path/alphalake.duckdb \
  --period 2026-06-30 --as-of 2026-09-09T18:00:00Z \
  --policy batch-policy.json --output-dir ./data/batch_runs
```

`batch-policy.json` 包含 `policy_version`、`review_note`、`assignments`。`assignments` 按六位代码索引，每项包含现有完整 `policy` 和可选 `wacc_binding`，与单公司 API 契约一致。可将既有示例政策装入任务：

```python
import json
from pathlib import Path
policy = dict(policy_version='reviewed-2026H1-v1', review_note='仅已审核双公司，其他公司不外推', assignments={})
for code, name in [('300866','anker-2026H1-revised.json'), ('600519','moutai-2026H1-central.json')]:
    policy['assignments'][code] = dict(policy=json.loads(Path('../examples', name).read_text()))
Path('batch-policy.json').write_text(json.dumps(policy, ensure_ascii=False, indent=2))
```

批量命令直接调用与 HTTP 相同的 `evaluate`，无需启动 API 服务。先扫描本地分母，再逐证券导出同一请求期间/信息截止的标准数据；每份证券导出有独立事务，整批不是跨证券/跨库原子快照，应在同步完成后运行。

缺政策、缺项、错误身份/政策和执行失败分别列示。单公司成功结果继续保存在 `ALPHALAKE_VALUATION_RUN_DIR`（默认后端 `data/alphalake_runs`），按输入/政策/引擎版本寻址；每次批量尝试另存独立 JSON，保留失败记录。重跑会重新检查数据并重试失败，成功内容不覆盖；当前仍重新执行引擎，尚无计算缓存或守护调度进程。

这一步自动化执行已有政策，尚不自动生成全市场政策。双公司实际数据库试跑成功、默认测试覆盖重放、缺附注隔离和身份错配；它们不代表新增行业已经验收。

## 通用经营价值筛查

新增 `nonfinancial-earnings-power-v1`，API 与批量 assignment 均可使用，不绑定特定证券代码。政策示例：

```json
{
  "policy_id": "nonfinancial-earnings-power-v1",
  "approved_report_period": "2026-06-30",
  "scenario": "zero-growth-10pct",
  "review_note": "零名义增长、维持性投入等于折旧的经营价值敏感性；非市场目标价",
  "nonfinancial_scope_review": "需填写公司属于适用非金融经营范围的审核依据",
  "wacc": 0.10,
  "tax_rate": 0.25
}
```

自动读取并核验 TTM FN230/86/305/306/83/82/301，计算 `EBIT=营业利润+利息费用−利息收入−投资收益−公允价值变动收益−资产处置收益`。正收入、正 EBIT 且 EBIT 不超过收入才进入共享引擎。已知非零金融业务收入/支出或吸收存款字段拒绝；这些字段缺失不等于金融业务不存在，仍须显式范围审核，尚不自动全市场分配。

十年预测和终值保持零名义增长、当前调整后利润率、政策税率和 WACC，净再投资为零。它是“维持性资本开支等于折旧”的假设，**不是历史 FCFF 已闭合，也不是未来增长预测**。销售资本倍率在零增长下不影响结果。模型结果应等于 `EBIT×(1−税率)/WACC`，测试用这个独立公式核验。

输出状态为 `illustrative_enterprise_value_only`，与完整条件估值分开统计；每股值及历史 FCFF 仍为空。没有补零或隐式赋予现金、债务、少数权益、期权任何数值。套期经营影响尚未恢复、研发仍费用化、维护投入与税率/WACC均为情景；不能将此筛查结果当完整自动估值。生产 API 的真实安克标准链已通过，真实茅台因金融业务被拒绝。

## 2026-09-09 首轮真实市场运行

隔离库 `workspace/auto-valuation-20260909/market.duckdb` 经生产 `sync-instruments` 同步 52,480 个各类证券，三个分区均成功。财务扫描分母为 5,569 只股票：XSHG 2,320、XSHE 2,903、XBSE 346。该新库初始全部缺标准事实，这是空财务库的基线，不是 TDX 没有财务数据。原双公司验收库未改动。

北京代码请求首台超时后第二台恢复。首次财务包同步列出 147 包，默认选择 2026H1；协议三台均空响应，官方 HTTP 下载 45 秒超时，运行失败保留，未导入部分财务事实。正在继续补归档恢复、公告与有限历史回填，尚无全市场估值成功率可报告。

财务有限回填可使用 `sync-financial DB --latest 6`，复用既有按新到旧选择逻辑并排除未来占位期，不必下载全部 147 个历史包。默认仍只取最新一期，`--all` 与 `--latest` 互斥。TTM 需要跨季度和上年同期，具体窗口完整性仍以扫描结果为准。

全市场实测另发现 CNINFO 连续页只重复既有公告身份。现在保留异常响应并拒绝完成窗口，检查点键升为 v4，旧 v3 不再作为跳过依据；历史事实与旧证据不删除。官方 TDX HTTP 财务 ZIP 实测慢速传输超过 45 秒，因此 ZIP 下载上限改为 10 分钟，清单仍 45 秒，父上下文取消、大小和 MD5 校验继续生效；三节点协议尝试次数不变。

同窗口进一步控制变量实测：pageSize=30 两页各30条且无交集，50/100两页各30条且完全重叠。默认页大小从50改为30；不推断服务器内部原因。已保留三份原始响应，并用真实对照/旧检查点失效/重开检查回归。

## 深分页自动恢复

每页30条的全市场运行仍在5个大日期窗口发现后续页面停滞（首个为2025-04-01至2025-06-29第58页）。流程现在自动将该窗口二分后分别重新采集，直到可完整遍历，或细分到单日仍失败。父窗口原始响应与拆分诊断保留，父窗口部分公告不发布；只有成功子窗口获得检查点。单日失败仍计入运行失败，不用部分结果冒充完整目录。

日期二分最初沿用v4，后续部分重叠实测已将完成键升级v5（见下文）。重放会先探测尚无完成键的父窗口，然后复用同版本成功子窗口检查点；目前不缓存分割计划。回归覆盖真实重复页的单日拒绝、二分后的两子窗口完成、诊断保留及重放跳过子窗口。

最新全量2026H1包另含两对完全相同的证券行，原逻辑因此拒绝全包。生产解析继续保留全部原始行，协调阶段现仅合并完全相同的重复记录；冲突处理已进一步改为证券隔离（见后文），原包和五行真实裁剪回归保留。这不改变80个FN的语义或字段位模式。

## 通用每股 DCF 与历史规则预测

`nonfinancial-book-fcff-v1` 接受显式十年 `annual_forecast`，复用标准窗口校验与完整共享引擎，从 FN238、FN133、FN41/52/55/56/439、FN69 自动构造财报日股本、现金、债务和少数权益桥接。输出单独标为 `illustrative_book_equity_scenario`。非正股权剩余拒绝并要求困境/期权模型，不给负股价或默认为零。

其取舍必须逐项提供：现金回收率、经营现金准备、债务/少数权益账面倍率、额外稀释、附加索偿情景，以及明确“不转股”和“未分类金融投资不计入”。FN52 全额按债务代理；没有把未分类投资宣称为零，也不把附加索偿情景当成披露事实。它尚不闭合当前证券滚动、转债选择权及其他索偿公允价值。

`nonfinancial-history-fcff-v1` 进一步自动生成预测，模板见 [`nonfinancial-history-template.json`](../valuation/examples/nonfinancial-history-template.json)。规则为：

1. 读取截止报告期最近四季度内可获得的同季度收入同比，至少两组；每个历史源值也核对原始位模式、币种、季度期间和公告血缘。
2. 同比中位数加情景偏移后按显式上下限截断，作为前五年增长，第六至十年线性过渡到终值增长。
3. 从 TTM 调整后 EBIT/收入取得当前利润率，五年内过渡到当前值加显式偏移。税率、销售资本倍率、WACC及稳态ROIC仍由政策给出。
4. 自动生成完整十年政策，保存历史同比分量及源事实ID、观察中位数、截断后增长、生成政策和规则哈希，再走同一 DCF/股权桥接。

这是机械初始情景，不是研究预测；模板的10% WACC、25%税、3倍销售资本等都没有被称为全市场合理参数。操作人仍需审核非金融经营范围及配置政策；未自动分配给全部证券。历史增长样本不足、源位篡改、已知金融业务、缺股本/现金/债务及终值无效均拒绝。安克真实标准链已完成逐年现金流、终值和每股值的独立复算；茅台金融业务拒绝。跨公司新增样本验收和自动行业路由仍继续推进。


## 行业规则自动分配

批量政策支持 `industry_rules`：每条规则提供唯一可识别的 `rule_id`、来源 `source`、分类体系 `taxonomy_code`、精确的 `node_codes` 清单、`max_age_days`（1–366天）、审核说明 `review_note` 和完整的 `nonfinancial-history-fcff-v1` 政策。`assignments` 可以为空；已有公司专用配置优先。规则中的非金融范围与所有参数仍须审核，行业归属不会自动证明参数合理。

扫描同时输出数据库里的行业成员证据：节点、有效起日、最近实际观察时间及观察运行完成时间。只接受截止时点已完成或部分完成运行中实际写入的行业成员，不因另一体系成功就刷新缺失成员。匹配零条或多条规则分别记录 `blocked_no_reviewed_industry_policy` / `blocked_ambiguous_industry_policy`；过期、未来或未发布证据不能触发估值。命中的规则和证据进入批量审计报告，模型自身的金融业务与数据缺项检查仍生效。

迁移032新增最近观察时间/运行，首次观测不变；旧库仅为尚未关闭的成员从原始观察回填，绝不把迁移日当同步日。`sync-industries` 对再次看到的成员刷新时间，不刷新部分快照中缺席的成员。当前只保留最近一次观察，早于它的历史截止可能缺行业，**不支持完整历史行业回测**；如需历史路由，应增加不可变逐次观察日志。先通过 `alphalake init DB` 或写入同步命令应用迁移；只读扫描不会静默迁移。

离线回归覆盖未完成运行隔离、重复观察刷新而首次观察不变、部分快照不刷新缺席成员、行业匹配/过期/冲突/公司覆盖/坏日期隔离。该路由测试的行业成员为合成数据，财务输入为真实安克导出，尚不代表全市场行业配置或新增公司原文验收完成。

## 有限内存部署

全市场六期重放在本机约4GiB内存环境实测被 Linux OOM 终止（PID2416089）；数据库恢复后源事实仍为0，未把中断当成包完成。已根据内核证据将遗留运行7记为失败。外部强杀不能执行进程内终态回调，日常应检查 `status` 中长期 running 的记录，并核实进程是否仍存活，不能仅按运行时长断言失败。

支持驱动原生资源参数：`ALPHALAKE_DUCKDB_MEMORY_LIMIT`、`ALPHALAKE_DUCKDB_THREADS`。不设置时保留 DuckDB 默认；非法值在打开库时拒绝。低内存机器可先按实际空闲资源设置，例如：

```bash
ALPHALAKE_DUCKDB_MEMORY_LIMIT=768MiB ALPHALAKE_DUCKDB_THREADS=2 GOMEMLIMIT=256MiB \
  alphalake sync-financial ./alphalake.duckdb --latest 6
```

DuckDB 限制与 Go 的 `GOMEMLIMIT` 各管各的内存，二者都不是整个进程 RSS 的硬上限。中间计算可溢写，但不能溢写的操作仍可能明确报内存不足；需保留磁盘空间并根据实测调整，不能将上述示例视为全量导入容量保证。原始归档和事务回滚仍承担失败恢复，不能通过少导源字段来隐瞒容量问题。

## 一次刷新到估值

在 `valuation/backend` 下运行：

```bash
ALPHALAKE_DUCKDB_MEMORY_LIMIT=1536MiB ALPHALAKE_DUCKDB_THREADS=1 GOMEMLIMIT=192MiB \
python -m tools.refresh_valuate_alphalake /absolute/path/alphalake.duckdb \
  --period 2026-06-30 --filings-start 2025-04-01 --latest 6 --stage-timeout 21600 \
  --policy /absolute/path/batch-policy.json --output-dir ./data/refresh_runs
```

顺序是 `sync-financial` → `sync-bse-code-transitions` → `sync-filings --metadata-only` → `sync-industries` → 首次 `materialize-fundamentals` → `repair-filings` → 再次物化 → 可选WACC与资本效率参考源刷新 → 批量估值。证券名单由同步命令刷新；不另建常驻服务。可由本机 cron/systemd 定时调用此命令，尚未替用户安装周期任务。进程锁按数据库绝对路径保护同一入口的重叠运行，其他直接写库命令仍依赖 DuckDB 自身锁；不支持跨主机调度锁。

`--filings-end` 默认中国当日，`--as-of` 默认取本轮同步/物化结束后的实际时点，避免刚刷新的行业因晚于启动时点被误排除。报告期及对应审核政策仍须显式更新，默认六期回填不能保证任意历史报告期齐全。已嵌入的WACC参考包仍须符合本轮信息截止和陈旧约束；加 `--reference-database /absolute/references.duckdb --sync-references` 可自动刷新并固定六类参考版本（WACC四源、行业资本效率、公司行业）。行情和所有公司市场股债结构尚未纳入该通用周期，行业风险暴露及目标权重仍属显式政策。

每次运行保存独立目录：逐阶段日志、持续原子更新的 `run.json` 和批量报告。采集失败不阻止其他采集及缺项检查，但总任务返回非零；物化失败则跳过批量估值，避免将旧物化结果当作本轮成果。`completed` 仅表示命令执行完成，估值覆盖率仍看批量报告，全部公司被阻断也不能称为估值完成。

单阶段默认上限3600秒。78行业全市场批次在本机实际超过一小时，上例显式设为21600秒（六小时），这是部署超时政策，并非完成时长保证；可通过 `--stage-timeout` 按真实市场规模调整；超时先发送中断、等30秒后才强制结束，退出码124保留。SIGTERM/交互中断记录取消。进程被系统强杀时只能留下 running 的审计记录，须结合实际进程状态诊断。离线检查覆盖采集失败后继续检查、物化失败阻止估值、逐阶段日志保留和真实子进程超时结束；完整市场流水线的在线验收仍在推进。

财务同步每个失败包现在立即输出包名和具体错误，失败计数和最终运行状态仍保留；因此长批次后续下载期间也能定位前一包的失败阶段，不必等待只含首错的终场摘要。

容量复现进一步将失败定位到 `provider_record_resolution` 的逐条 UPSERT；现改为标准库 JSON 参数加单个 `INSERT SELECT ... ON CONFLICT`，仍在同一事务发布，既有人工确认保留/重新解析后清除的规则不变，重复原始代码明确拒绝。新增6000条记录、128MiB DuckDB限制下的写入与重放回归。

另以真实全包数值、合成证券ID做独立容量诊断（不能作为真实证券链验收）：约324万源字段在768MiB限制下于事实合并失败；1024MiB、单线程下写入成功，用时约44秒，峰值RSS约1.93GiB。生产全市场回放正在按新配置继续；这些容量试验不替代真实身份解析、公告关联或估值验收。


## 重复记录的冲突隔离

真实运行已导入五期、16,061,752条源字段；2025年报中300750两行只在FN337冲突，原先因此整包失败。现在保留原始替代值、将该证券整体标为待解析，让其他证券继续发布；不选择13.67或30中的一个，不将冲突列补零。待解析仍阻止包完成检查点，CLI 对存在待解析或主数据分区失败的 partial 同步返回非零退出码，便于周期任务发现问题。

迁移033提供 `provider_conflicts_asof`，按源观察时间选择同证券/报告期最新已观察记录；历史源事实仍保留。扫描和导出增加 `source_conflicts`，批量及单公司正式入口都阻断仍有冲突的证券，不能因为旧版本字段齐全而回退旧值宣称完成。新版本真正恢复无冲突状态后才解除其后时点的阻断。扫描范围与导出一致：目标报告期前一年的1月1日至目标报告期。

当前按证券隔离，即使冲突发生在未审核字段也不将该证券交给模型；逐字段共识尚未实现。真实四行裁剪包验证隔离、无检查点、归档重用和重开，合成后续观察验证时间边界，真实安克旧完整输入上的负向测试验证 API/批量不得绕过质量门槛。


## 行业未知成员与部分发布

真实全市场申万快照包含无法在同轮证券主数据中解析的 `sz300728`（X150202）。此前整个体系因此回滚。共享分类写入现在记录 `classification.unresolved_member` 诊断，发布可确认成员，并将该快照视为不完整：**不关闭任何既有成员区间，也不刷新未观察到的成员**。行业与概念两路都将运行标为partial，命令非零退出；`synced` 计数表示有提交，可能同时有该体系的不完整诊断，不能与failure计数相加理解为独立集合。

严格的身份歧义仍回滚整个事务；源叶节点缺名称等构建错误仍拒绝该体系（本次另有TDX的T00缺名称），不伪造身份或名字。CLI逐个输出体系问题，而非只保留首错。旧归属在持续不完整快照下可能保持开放，因此行业政策仍检查最近实际观察与年龄，归属冲突继续阻断。

真实适配器输出裁剪覆盖未知成员诊断、9个可确认成员发布和扫描可见；它不是原始协议文件回放。另有128MiB下6000成员和未知成员的重复刷新回归，旧归属保留由独立区间测试锁定。现有主数据不匹配的原因尚未推断。

本次年报补入3,246,456条，六期累计写入19,308,208条源字段；其余五包按已验证检查点跳过。年报仍因300750冲突保留1项待解析、无完成检查点，命令如实返回partial。全市场公告的日期细分采集继续运行，标准事实完整率与新增公司估值尚待后续实际物化/验收，不将源字段总数当估值覆盖率。

## 新公司核验与审核隔离

海天味业、汇川技术已通过六期真实TDX包→公告关联→标准物化→时点导出→共享估值入口的离线回归，十年预测/终值/股权桥接独立公式复算通过。八份原始PDF的65组金额/比较中58组源位相符、7组不同；海天季度重述及两家公司后续比较列差异保留，标准事实不被PDF覆盖。三项报表空白债务没有独立数值零证明，可比历史和完整估值验收仍未闭合。详见[原文、版式和差异台账](../internal/ingest/testdata/generic-valuation-2026/README.md)。

批量政策可增加 `exclusions`，如 `"exclusions":{"603288":"后续比较列与原披露不同，可比历史待审核"}`。键为六位代码、值为非空审核原因；优先于公司指定和行业匹配，输出 `blocked_review_exclusion` 并保留证券分母，不执行导出或估值。政策整体随批次留存。实际四行业试跑政策已隔离这两家公司；这只是显式政策审核门槛，不是自动重述识别或直接API的全局质量禁令。机械情景能计算不代表历史口径通过。

## 部分页重叠与v5目录完成键

全市场日期细分运行13结束：98窗口、4,443页、51,575次公告写入尝试，插入32,860、更新18,715；8个单日窗口失败，未完成全市场目录验收。这些是本次写入操作数，不是去重公告数或上市公司覆盖率。

复测2025-04-29发现第6页仍有27个新身份、同时重复3个旧身份，旧“零新增才拒绝”不足以保护完成度。现在任何公告身份重复及末页累计行数小于上游宣称总数都会阻止完成；多日沿用日期拆分，单日保留失败。完成键升v5，旧v4不再跳过；已取得原文/公告不删除。六份原始响应及合成末页边界有离线回归，详见[证据](../internal/ingest/testdata/cninfo-partial-overlap-2026/README.md)。当前还需要补齐密集单日目录，不能将运行13或v4键称为完整快照。

## 按证券补齐公告

密集单日失败不必重扫整个市场：`sync-filings DB --code 300124 --start 2025-04-01 --end 2026-09-10 --metadata-only` 复用同一生产采集链，向CNINFO传入六位代码检索词。两份实际响应各11条且身份一致，离线客户端保护请求参数；响应意外包含其他证券时保留原始页、拒绝该窗口完成。代码查询和全市场查询的完成键/源定位符分开，代码补齐不能推进全市场窗口；旧代码窗口可幂等跳过，近期仍复扫。CLI逐项输出窗口失败详情。

实际汇川补采运行16通过六个窗口，11条公告全部解析，新增7条、更新4条；这直接证明宽窗口全市场运行尚有其公告缺口。它不解除已经记录的比较口径审核隔离，标准物化与覆盖率扫描仍须随后执行。

## 全市场标准就绪度实测

在运行15的1,412,740条标准事实之上，原全市场聚合在1 GiB DuckDB内存限制下失败；仅分行输出JSON仍失败。现于同一个事务快照中先读取证券分母，再按128只证券聚合标准TTM窗口，保持全部字段、缺项、血缘和排序；没有通过减少字段伪造覆盖率。129只合成证券跨批次、稀疏且大于2^53的证券/事实ID在128 MiB下回归，防止批次串号或JSON精度损失。

同库、同ASOF `2026-09-10T00:24:09Z`、同1 GiB/单线程限制下，真实扫描1分56秒完成，192 MiB JSON输出；进程峰值RSS约1.66 GiB（native限制不是整个进程限制）。5,569只本地已知A股中：核心齐全1,146（20.6%）、缺核心输入4,370、无标准事实52、源重复冲突1。这是15字段筛查，不是估值覆盖率或交易所全市场完整性认证；此次标准事实仍是运行15状态，后续代码补采公告还未重新物化进这份统计。

## 自动公告修复步骤

`repair-filings DB --period 2026-06-30 [--limit N]` 消费最近一次物化留下的关联缺口：仅选择目标期及前一年范围内、最新源版本仍为pending的证券；已被新版本取代的缺口和歧义不作为普通补采队列。按最早缺失报告期至当前中国日期逐公司查询，一次失败不阻断其他证券，取消时如实计已尝试数；每个子运行仍持久记录。默认尝试全部队列，`--limit` 只限制本轮数量。

刷新命令已串入“首次物化→公告修复→再次物化→批量估值”。任一次物化失败都阻止估值，补采部分失败仍允许第二次物化检验成功部分；最终ASOF在这些步骤之后取得。查询成功不等于关联修复成功，必须看后续标准窗口。独立使用修复命令前后也应运行物化。

实际首轮队列4,823家公司，先试100家，69家查询完成、31家被关键词检索返回其他证券拦下，剩余4,723家未尝试。这是部分执行验收，不是全量修复完成；精确机构筛选已用真实响应验证，下一步接入。

## 机构精确筛选

首轮关键词查询仅在两个样本上未混入其他证券，后续100家实测推翻了通用适用性。现在 `--code` / `repair-filings` 从有目录归档的公告复用唯一 orgId，以 `stock=代码,orgId` 且空 searchkey 查询；没有已知机构时，关键词只用于完整单页的唯一机构发现，不发布发现页公告。机构缺失、多值或发现页不完整均失败，不按代码构造 orgId。生产逐条复核代码和机构，源定位符和完成键加入机构标识，旧关键词完成键不代替精确查询。

四份原始响应离线锁定请求及身份，包含康佳 A/B 股分离，见[证据](../internal/source/cninfo/testdata/stock-catalogues/README.md)。存储复用、机构歧义拒绝、跨代码检查点及失败记录重开有回归。真实重跑100家（运行119–218）全部查询成功、无待解析公告；队列4,757家中4,657家尚未尝试。这是查询执行率，不是财务完整率，仍须重新物化验收。

## 通用模型消费 WACC 参考数据

三个通用政策（经营价值、账面 FCFF、历史规则 FCFF）现在也可省略 `wacc`，改由同一请求的 `wacc_binding={references,policy}` 计算；批量公司 assignment 可直接携带该绑定。继续复用现有参考库导出、完整性/时点/陈旧检查及共享 M2，国债、行业 Beta、ERP、国家风险和借款成本不再只供两个专用公司模型使用。WACC 政策代码须匹配当前证券，普通企业限定合并范围及显式目标资本结构；金融业务门控照常生效。

固定 WACC 与参考绑定必须二选一，最终 WACC 再次验证终值增长上限。合并 EBIT/TDX 总利息可在明确适用的大型非金融合成信用政策下生成借款成本，零利息及信用区间缺口不猜。目标股债比例、业务/国家暴露、税率仍是政策；没有将未知公司市值或类别股本补零。通用市场股债权重尚未验收，直接拒绝此绑定，原安克/茅台专项市场链不受影响。

安克真实标准财务与归档参考导出经过 HTTP、共享引擎及 Decimal 独立 WACC 复算；经营价值另以 EBIT×(1−税率)/WACC 复核，历史模型生成政策保留同一折现率。重复调用完全一致，双来源/无来源、错证券/范围、陈旧参考、终值越界均拒绝。Python 全套212通过、4项既有跳过，Go全套与构建通过，无依赖变更。这里证明接口自动衔接，不是全市场行业暴露或当前合理价格已验证。

## 首轮真实行业批次

运行219在机构精确补采100家后新增12,450条标准事实、200组源记录补齐关联，标准事实总数1,432,706。信息截止2026-09-10T01:24:27Z的全市场扫描得到核心齐全1,292/5,569（23.2%），缺核心4,224、无标准事实52、源冲突1。较运行15增加146家核心齐全；这个比例仍不是估值完成率。

四行业机械政策真实批次 `workspace/auto-valuation-20260909/batch-runs/batch-0ks3hw13.json`：4家账面FCFF情景完成（安记食品、鸿合科技、小熊电器、仲景食品），73家缺估值输入，4家因非正经营利润或非正股权残余拒绝，海天/汇川2家保持审核隔离；其余5,485家没有已配置行业政策，全市场另保留1家源冲突。四家公司仅通过标准数据→政策→引擎的实际执行，尚未逐家公司财报独立核验；不称新公司完整验收或公允价格。安克虽核心齐全，仍缺第二组季度同比，所以通用历史模型正确拒绝。

首轮批次还对已知缺核心的公司逐一重算导出，耗时约十分钟。现在账面/历史FCFF模型复用本次就绪度已确定的核心缺项，直接留 `blocked_missing_inputs`，无需再跑该公司的TTM导出；核心齐全仍走原标准导出，经营价值模型不套用股权桥接缺项门槛。跨时点同步可能改变后续批次结果，本批次不将后来补入的事实冒充扫描时已齐全。

全量机构补采已启动，队列4,657家；执行结束后仍须二次物化与重跑批次。流水线未取得这一轮全量完成结果前，不宣称公告或估值全市场闭合。

## 行业参考 WACC 与定时批次

行业规则现在可选择 `wacc_policy`：沿用既有 WACC 政策字段，但删除 `code`（或显式 null），同时删除该规则经营政策中的固定 `wacc`。行业模板只承载代理行业、国家暴露、目标股债结构等选择；确认证券的行业证据后，系统才绑定其六位代码。模板不得携带某一家公司的代码，须为同期间、合并范围、目标资本结构；固定 WACC 与参考模板不能并存。

批量政策可嵌入 `wacc_references` 固定包，或者运行时提供参考库（两者互斥）：

```bash
python -m tools.batch_valuate_alphalake /absolute/financial.duckdb \
  --period 2026-06-30 --as-of 2026-09-10T01:24:27Z \
  --policy industry-policy.json --reference-database /absolute/references.duckdb \
  --output-dir ./data/batches
```

后者用本地 `export-wacc-references --latest` 按同一时点选择四类版本，并将完整固定包写入批次政策记录。缺少参考包的规则留 `blocked_missing_wacc_references`，不执行该公司导出；自动参考库导出失败则整个批次命令失败，不使用隐式旧包。每家公司仍执行身份/期间/参考陈旧/金融范围/终值条件检查，显式公司政策和审核隔离优先级不变。

`refresh_valuate_alphalake` 同样支持 `--reference-database` 并传入物化结束后的批次，适合定时调用。它此时只消费本地参考库，不自动对行业进行风险研究，也尚未在该选项下同步外部参考源。普通市场股债结构及类别股本仍未通用化，结果保持条件情景标识。

真实 CLI 衔接在隔离库 `generic-reference-live.duckdb` 完成：从既有双公司财务验收库复制，再实际刷新主数据/行业（5,569条已识别归属、7条身份缺口、TDX分类缺名称，运行保持partial）。同一时点2026-09-10T01:47:39Z，自动选出四类参考1/2/3/4，品牌消费电子模板匹配后绑定安克，完成1个条件账面FCFF情景；同行业其余11家缺输入，5,557家无此行业政策。此隔离库用于验证接口组合，不能拿它统计全市场财务完整度。

输出的目标权重 WACC 为6.9721%，不是安克当前市场化WACC结论。独立 Decimal 从所选参考与标准窗口重算 WACC、十年FCFF、终值和股权桥接全部一致；这项算术复核不新增PDF语义证据。原增长上限、目标结构、国家权重及账面桥接仍是机械政策，不等于已研究合理价格。Python213通过/4既有跳过，刷新参数传递5项回归通过；Go测试及构建通过，无依赖变更。

## 参考源也纳入刷新周期

在 `refresh_valuate_alphalake` 增加 `--reference-database /absolute/references.duckdb --sync-references`，财务二次物化成功后顺序刷新国家风险、全球行业Beta、人民币国债曲线及合成信用利差，再取统一信息截止进入批量估值。单独 `--reference-database` 仍只读取本地参考；`--sync-references` 没有参考库会在参数阶段拒绝。

子进程使用当前后端 Python 和仓库解析脚本绝对路径，避免在 `valuation/backend` 运行时找不到 Go 默认相对脚本。每个参考阶段独立记日志、退出码和起止时间；失败不改写原发布版本，后续批次仅能选择仍完整且通过陈旧检查的本地数据。只要某阶段失败，整轮保持partial，不把沿用旧数据描述成刷新成功。

真实参考库副本逐源重放：国家风险10项、行业Beta376项、国债8项、信用15项均成功，四类原始内容未变，均复用既有1/2/3/4版本，没有重复发布。来源适配器的每个原始响应仍归档。调度测试覆盖参考源失败后继续批次、绝对解析路径、解释器与参数传递、截止时点和取消/超时（6项）；Python全套214通过/4既有跳过，Go全套及构建通过。

## 小熊电器的原文审核

新增5份原始PDF、6期单记录裁剪与35组比较，走同一Go生产源解析/物化/时点导出并从Python进入共享引擎；35组中34组源位一致，1组为年度主表利息与附注利息/租赁两行加总不同。半年报两行加总与主表相等，不能把这种列示差异误判为TDX解码错误，也不能未经政策审核把附注金额加回标准FN305。股份数量取股份变动表，避免用未同步体现注销的旧股本金金额代替股份总数；完整[证据和版式说明](../internal/ingest/testdata/bear-valuation-2026/README.md)已进默认CI。

实际四行业政策 `shenwan-initial-screen-2026H1-review2` 将002959列为审核隔离，原批次4家机械结果仍作为历史尝试保留。源事实和主表值不改写，后续融资口径须单独协调；通用审计边界也明确不自动保证报表利息包含一致的租赁分量。Go全套/构建/vet及Python215通过（4项既有跳过），独立PDF校验通过，无依赖变更。

## 全量机构补采后的物化与重放

全量队列4,657家已全部尝试，运行220–4876共266家查询失败（244家官方旧代码、22家无唯一机构）；这是查询结果，不直接等于财务完整度。运行4878将标准事实从1,432,706增至1,991,593：新增559,648、更新1,766、删除761。33,062组源记录中32,045已关联公告、945待关联、72歧义，歧义不任意选公告。

运行4877曾在1GiB限制下合并事实失败，canonical事务回滚；成功的公告关联更新仍单独保留。运行4878用1536MiB DuckDB、单线程、192MiB Go限制完成，耗时99秒、进程峰值RSS约2.41GiB。因此上面的周期命令采用1536MiB实例配置，仍需根据机器实际空闲资源调整；不再把仅适用于更小数据量的768MiB例子用于全量周期。

运行4879在相同1536MiB限制下重放，新增/更新/删除全零。此前即使这三个内容计数为零，仍会执行整表UPSERT并刷新事实的运行血缘。现在复用事务内已算出的新增/更新计数，完全不变时跳过UPSERT；过期事实删除与本轮拒绝诊断继续执行，未提前返回。回归锁定未变事实的原始写入运行，已有更正、倍率修复和删除回归继续通过；全套Go测试、构建、vet通过。

改动后的真实运行4880在1024MiB原生限制下重放成功，事实新增/更新/删除仍全零，用时91秒、峰值RSS约1.88GiB。该结果只证明这次无变化重放容量；有新增或修订的大批量物化仍按1536MiB实测配置执行。

## 2026-09-10 全市场批次结果

运行4878/4879后的同库批次 `batch-z7k8wtsi.json`，信息截止 `2026-09-10T02:35:11Z`、报告期2026-06-30：5,569只本地已知A股中，15个核心字段齐全5,069（91.0%），缺核心字段469，无标准事实30，源重复冲突1。较补采100家时增加3,777家核心齐全。此分母按本地当前主数据与信息截止生成，没有独立交易所普查证明，也不是财报所有字段或估值完成率。

四行业83家中：43家完成机械账面FCFF情景，13家缺估值输入，24家被模型边界拒绝，3家审核隔离；另外5,485家没有匹配政策、1家源冲突。24家拒绝中15家不满足正收入/正EBIT，6家股权残余非正，3家不满足正股数及非负账面索偿/现金要求。没有将这些情况改成零估值或套正盈利模型。

本次原样使用的政策已提交为 [`valuation/examples/a-share-initial-2026H1.json`](../valuation/examples/a-share-initial-2026H1.json)，可传给批量/刷新入口的 `--policy`。它明确包含固定10%折现率等机械假设和三家公司审核隔离，并非市场WACC配置或已研究的行业合理参数；43个结果不是43家公司原文语义验收。参考WACC自动选择/同步的真实验收仍见上面的独立参考库链，不能说本批43家已全部改用市场化WACC。

下一主线按当前阻塞排序：扩展有依据的行业模型和参考风险政策；补齐适用公司的输入/可比口径；将北交所已核验的代码变更证据接入有时点和血缘的身份链。金融、亏损/困境和复合业务模型继续单独处理，不靠放宽门槛增加成功计数。

## 行业资本效率参考同步

为替代仅由人工指定的统一资本效率情景，新增原始参考入口：

```bash
alphalake sync-industry-capital ./references.duckdb --python /absolute/valuation-python
alphalake sync-industry-capital ./references.duckdb --offline --python /absolute/valuation-python
```

复用既有资本开支解析器、原始归档及参考发布事务，94个全球行业的收入/投入资本比进入`reference.industry_stat`，字段为`sales_to_invested_capital_ltm`、无量纲倍数，保留源样本量、单元格和原始浮点字符串。迁移034仅扩充该表白名单，保留旧观察ID和所有血缘；Beta和资本效率共用既有行业节点，没有自动创建A股行业映射。原文及[核验边界](../internal/source/damodaran/testdata/capexGlobal.README.md)随库保存。

真实参考库副本同步发布94条（新版本5），离线重放不新增；原有四源WACC在相同信息截止下导出与升级前完全一致。真实源观察日期2026-01-05，生产可用时间取2026-09-10首次取得时间，不把观察日期猜成发布日期。消费电子约1.91、食品加工约1.71，不能据此说任意同行公司必须采用该值。

该阶段完成的是原始参考同步/存储，当时尚未加入`--sync-references`四源周期或自动替换批量政策中的`sales_to_capital`。下一步以显式行业映射和采用理由接入参考选择及政策构建；未来增量再投资使用行业历史比率仍属政策，不能伪装成公司报表事实。

## 资本效率参考选择与估值绑定

`export-industry-capital references.duckdb --as-of RFC3339 [--release N] [--recorded-cutoff RFC3339]` 导出完整94行业参考包。指定正版本号可重放；省略版本号按观察日期、同日修订入库时间选最新。公开/系统时点、完整性、源单元格、单位、数值转换和发布血缘均检查；最新候选损坏时失败，不退回旧版本隐藏问题。

通用账面FCFF和历史规则FCFF请求新增 `capital_binding={references,policy}`。此时 `sales_to_capital` 必须留空；直接数值和参考绑定互斥。采用政策须明确证券、报告期、单个参考行业、映射理由、最低样本量、最大年龄、倍率及“以行业历史比率代理未来边际再投资”的理由。财务标准事实保持原样，采用值及完整参考/理由写入结果审计；绑定内容和新解析模块纳入输入/引擎版本哈希，重放可复现。

行业规则可携带不含公司代码的 `capital_policy`，匹配已观察的分类后才绑定证券。批量政策的 `capital_references` 可固定参考包；使用 `--reference-database` 时，存在资本效率规则便自动导出该库参考，不能同时嵌入另一份包。缺参考时输出 `blocked_missing_capital_references`，不会改用3或跳过该条件。`--sync-references` 现顺序刷新原WACC四源和新增资本效率源，再取统一截止进入批次。

可用政策示例见 [`a-share-capital-reference-2026H1.json`](../valuation/examples/a-share-capital-reference-2026H1.json)：保留四行业的其余机械假设及三家公司审核隔离，仅对消费电子、调味品分别明确采用全球消费/办公电子、食品加工的宽行业代理。没有声称Damodaran给这些A股公司作了该分类，也没有把固定10%WACC改称市场WACC。

[来源方法说明](https://pages.stern.nyu.edu/adamodar/New_Home_Page/databreakdown.html)明确其资本化研发和经营租赁会影响投入资本等指标。通用模型仍费用化研发、保留报告的融资口径；本次采用属于带口径差异的政策代理，尚未自动协调，不称作公司已验证的实际边际资本效率。该边界进入审计并有回归断言。

单公司真实主库导出→参考绑定→共享引擎已完成：同一安克财务快照与固定10%WACC机械情景，资本效率由3改为源参考1.905898248522；每股情景值由92.15变为71.01，仅用于检验这项假设的影响，不替代既有安克专项估值。独立Decimal从标准窗口、季度同比和原始参考值重建十年FCFF、终值及账面股权桥接，结果一致；不是新的PDF语义复核。

2026-09-10 03:17:03 UTC，并发Go/Python全套与真实批次验收触发本机内存不足，内核终止AlphaLake进程2491816；没有生成完整批次报告，未计为成功。后续改为串行重跑，避免将1GiB DuckDB限制误当作整个进程或多进程的内存上限。

串行验收已完成：Go全套/构建/vet、Python223通过/4既有跳过；五参考源真实刷新全部幂等。实际批次`batch-khvpixho.json`，截止2026-09-10T03:29:10Z，5,569家分母及各阻塞计数与前批一致，仍为43家条件账面FCFF、13家缺输入、24家模型边界拒绝、3家审核隔离、5,485家缺行业政策和1家源冲突。43个结果中18个采用资本参考，25个保留直接政策比率；逐公司比对恰有这18个结果变化，安克为71.00508238257007。此轮验证自动参考选择和政策传递，不增加公司原文验收数量或公允价值覆盖率。

## 单公司导出与全市场扫描容量

迁移035给共享TTM宏增加可选证券范围，将过滤放进事实CTE；原二参数全市场调用仍可用。就绪度复用128证券批次，单公司导出从该代码所有标准事实求身份范围，最终仍按原代码与版本规则筛选。没有提前按代码挑旧版本、只认当前身份或舍弃旧期缺项窗口；已有库须先执行`alphalake init DB`，不需要重物化。

真实主库单次对照（同一2026H1报告期、截止2026-09-10T03:03:51Z，native1024MiB/1线程/GOMEMLIMIT192MiB）：安克完整导出从8.09秒、RSS约896MiB降至1.60秒、约481MiB，JSON逐字节一致。执行计划试验确认范围进入事实读取后，TTM版本排序从1,991,593行降到安克386行；不是将源证据删掉或缓存结果。单次时延不是稳定吞吐保证。

5,569家公司扫描在截止2026-09-10T03:29:10Z约39.66秒完成，峰值RSS约1.07GiB，较此前约两分钟下降。完整就绪度的规范JSON SHA256仍为`ee8f7bcfa0ab99a90e14dc3e4ac5c7d67efce64cb3a37fefc432983c2825d2ce`，与优化前完整批次保存值一致；包括字段值、缺项、分类和血缘，非仅比较总数。核心齐全5,069/5,569不变。

Go全套/构建/vet及diff检查通过；组合、更正、缺期、单位错误同时走全量/限域查询，新回归锁定同码多身份、大整数ID、其他代码/来源取代、未来公告和旧期缺项。初次全套因workspace的临时性能程序同包重名编译失败，已加构建排除并完整重跑通过。本地主库中的未提交试验宏也已替换为最终035定义，标准事实仍1,991,593条、最终导出如上述完全一致；提交迁移不包含试验接口。

## 官方公司行业名单：来源解析

新增官方`indname.xls`解析器和[原始压缩证据](../internal/source/damodaran/testdata/indname.README.md)，复用已有xlrd依赖及冻结脚本执行边界。三张工作表各48,156条记录，行多重集合一致；94个行业与既有参考行业目录相容。解析5,100条沪深上市标识（SHSE 2,225、SZSE 2,875），12条无ticker保留在原始证据并计数，不猜身份；其余市场未开放解析。名单没有明确来源日期，保持空值，不能借用Beta/资本效率日期。当前解析器限定已审核的范围，源结构或记录规模改变时拒绝并待重新核验。

该名单提供的是来源分类：安克为`Computers/Peripherals`、小熊为`Furn/Home Furnishings`、汇川为`Machinery`，不等于此前为特定情景明确选定的宽行业代理。不会静默覆盖已有政策。`Country`也不能替代上市地；安道麦和中国中铁分别保留Israel/Hong Kong及其沪深上市标识。源SIC中的两个数值0保留原类型，不转成空值。

此阶段尚未写入生产分类或增加估值成功计数。接续链路须先独立发布来源分类观察，再按业务时点匹配本地证券，分别记录源快照完成与身份未解析；不能在存在未解析身份时推进“全部分类完成”检查点。匹配后仍须审核模型适用范围、风险/再投资采用政策及已有三家公司隔离，不能把5,100条标识直接视为可估值A股数。

## 公司行业名单来源发布（迁移036）

```bash
alphalake sync-company-industries ./references.duckdb --python /absolute/valuation-python
alphalake sync-company-industries ./references.duckdb --offline --python /absolute/valuation-python
```

命令复用原始归档、冻结解析器、参考版本及原子发布链。5,100条来源观察进入`reference.security_industry`，保存版本、归档、来源行、原始交易所ticker、行业节点和保留类型的原始行载荷；Beta、资本效率和该名单复用94个行业节点。没有在来源表填入推测的标准证券ID，也没有修改`classification.membership`、财务事实或估值政策。

数据集为`shse-szse-company-industries-v1`，与后续本地分类物化的运行/完成状态分开。版本的`source_version`和`source_published_at`为NULL，首次归档时间仅作`first_seen`可用时间，不声称是来源发布日期或行业生效日。该名单与2026行业目录相容不等于已证明名单日期。重放逐条检查原始行及行业映射，并核验完成键；不能靠重放修复被篡改的既有观察。

离线参考重放先选择最新已发布版本，再检查其归档关联；最新版本缺血缘时明确失败，不通过内连接默默选回旧版本。下载失败/坏工作簿仍保留失败证据，不发布新版本、不推进该来源完成键。公司名单真实文件大于16MiB，下载上限仅为该已审核数据集扩展到64MiB；其他Damodaran源保持既有上限。全部来源发布完成不等于本地证券身份全部匹配。

真实独立库在线同步运行1发布5,100条、版本1，离线运行2仍为版本1且无新增；重开直查schema36、94节点、1个来源完成键、0个标准分类成员。实际原始SHA256与提交压缩归档完全一致，`source_version=NULL`，`available_at=first_seen_at=2026-09-10T04:14:24.014653Z`。Go全套（含实际归档重放、坏响应、观察行/检查点篡改、损坏最新版本拒绝）、构建和vet通过；无依赖变化。此验收证明来源观察链，不将其计入估值公司覆盖率。

## 公司行业的标准查询投影

`valuation-readiness` 现按查询时点选择已完成发布的公司行业版本，在同一只读事务中校验来源行、目录、归档关联、完成键及规范化内容摘要，再投影到本地标准证券身份。批量报告保留 `company_industry_reference` 状态及逐成员血缘；无需另外复制一套可变成员表，不修改财务事实或既有申万归属。参考版本损坏时明确隔离该来源，不退回旧版，财务就绪度仍可返回。

名单没有来源生效日期，只能按首次取得日（中国日期）的半开 TDX 标识区间匹配；同时要求该代码在当前已知身份历史中仅属于一个证券。跨生命周期复用、区间重叠、交易所冲突、非 A 股及缺身份分别保留状态，不用国家字段或公司名称猜身份。这是当前主数据知识下的首次观测投影，不是历史主数据快照或来源行业生效日证明。

规范化版本升级为 `source-company-industries-undated-v2`，摘要锁定完整解析内容；旧 v1 不满足查询校验，执行 `sync-company-industries DB --offline --python /absolute/valuation-python` 可从已有归档发布 v2，保留旧版本。摘要只证明内容完整性，不代替独立语义核验。独立真实库升级运行3产生版本2、复用归档1，新增5,100条版本观察。

真实财务主库运行4881发布该来源后，在 `2026-09-10T04:42:23Z` 查询：5,100条中5,071条关联当前 A 股、10条范围外、19条待解析；其中4,957家公司同时具备核心财务字段。全体5,569家公司财务状态仍为5,069核心完整、469缺字段、30无标准事实、1源冲突。新增行业查询尚未扩大既有43家条件估值结果，不能将5,071关联或4,957交集冒充已估值或逐公司PDF验收数。

真实归档身份边界测试进入CI，覆盖大整数ID、首次可用边界、未来区间、跨证券代码复用、交易所冲突及有效JSON篡改；Go全套、构建、vet通过，Python全套224通过/4项既有跳过。现有政策仍需显式选择来源行业和适用模型；首次取得时间不因重复取得相同文件而刷新，也不证明上游名单近期更新。

日常 `refresh_valuate_alphalake --sync-references` 现刷新六类来源：原有WACC四源、行业资本效率、公司行业。公司行业写入财务主库（身份查询所在库），其余写入指定参考库；单来源失败保留阶段失败状态，后续批次仍按已发布版本、时点和政策年龄门槛判断，不声称使用本轮失败的快照。调度测试覆盖目标库、绝对解析器路径、失败隔离和最终时点。

新增 `valuation/examples/a-share-damodaran-five-2026H1.json`，仅采用来源名单中的Computers/Peripherals、Beverage (Alcoholic)、Food Processing、Furn/Home Furnishings、Machinery五个非金融行业。全球行业资本效率按相同来源目录绑定，固定10% WACC及既有历史经营外推仍为显式机械情景；保留三家公司审核隔离。七天首次取得采用窗口不等于来源名单更新日期。实际批次覆盖结果待验收，不把政策文件的存在计为完成。

## 五行业真实批次验收

`a-share-damodaran-five-2026H1.json` 已在真实主库、参考库及同一截止时点 `2026-09-10T04:42:23Z` 完成运行。报告为 `workspace/damodaran-five-batch-20260910/batches/batch-tzoypjky.json`，当前本地A股分母5,569：五行业覆盖687家，其中307家条件股权估值、13家缺输入、364家输入/政策拒绝、3家既有审核隔离；另外4,881家未分配行业政策、1家源冲突。没有执行失败或吞掉未返回公司的情况。

| 来源行业 | 条件估值成功 | 缺输入 | 输入/政策拒绝 | 审核隔离 |
| --- | ---: | ---: | ---: | ---: |
| Computers/Peripherals | 16 | 1 | 27 | 0 |
| Beverage (Alcoholic) | 21 | 2 | 14 | 0 |
| Food Processing | 57 | 2 | 73 | 1 |
| Furn/Home Furnishings | 29 | 1 | 46 | 1 |
| Machinery | 184 | 7 | 204 | 1 |

364家拒绝分为：收入/调整EBIT非正203、股权残值非正107、股本或账面索偿/现金符号检查47、金融业务字段FN506命中7。不能通过取消检查把这些公司计为成功。茅台属于金融业务拒绝，需要已审核的酒业/财务公司拆分；来源行业为酒类不证明合并口径可直接套普通模型。

307份落盘请求均以独立Decimal程序重建标准事实TTM、季度同比中位数、十年收入/再投资/FCFF、终值及账面股权桥接，每年输出和每股值全部通过，且run ID集合与批次成功集合完全一致。程序及结果保留在同目录 `independent_check.py`、`independent-final.json`；它们是本地验收产物，依赖本地落盘请求，不是新增307家公司PDF语义验收。历史FCFF仍为空，所有未来现金流均来自显式政策。

安克在本次固定10%机械情景为84.49046元/股；采用来源真实分类Computers/Peripherals的行业资本效率，与此前申万消费电子代理不同。这是批次链路对照，不替代安克专门研究估值或当前目标价。下一步比较参考驱动WACC，并针对实际缺项/模型边界继续扩展；成功覆盖率不能用核心财务完整率代替。

## 扩展行业准入政策

新增封闭的78行业普通非金融机械情景准入清单 `valuation/examples/a-share-nonfinancial-recipe-2026H1.json`。94个现有来源行业必须逐一归入准入或带理由的隔离，重复、漏项、未知行业及源目录新增项都会拒绝展开。16个隔离行业包括9个金融、6个住宅开发/地产/REIT及Diversified；这些公司需要专门模型，未通过改行业名称绕过。

在 `valuation/backend` 执行：

```bash
python -m tools.prepare_industry_policy ../examples/a-share-nonfinancial-recipe-2026H1.json > /absolute/output/nonfinancial-policy.json
```

输出就是现有批次/日常刷新入口接受的 `BatchPolicy`，不新增估值运行接口。展开逐行业绑定相同来源目录的Beta、D/E及资本效率，保留经济参数、公司审核隔离和行业隔离理由；完整结果随批次固定版本与内容留档。目录中没有的行业不能被自动准入。单一模板减少重复政策文件，不将模板中的BBB信用档、中国风险暴露或25%税盾变成来源事实。

78行业是一般FCFF机械筛查的政策范围，不是逐公司业务/PDF审查结论：周期正常化、研发资本化、租赁、受监管收益和兼营范围仍有局限，既有缺项/符号/金融字段门槛继续执行。实际公司覆盖与成功数须另跑真实批次验收，不能按行业数推算完成度。

负少数权益的后续边界已取得一例原文证据：603029的2026H1合并资产负债表（PDF44页、元/人民币）列示少数权益−28,658.30元，TDX FN69标准值−28,658.30078125与其float32位一致；不是将负数误当缺项。归母950,137,729.66加少数权益闭合总权益950,109,071.36，仅为代数一致性。原文 `https://static.cninfo.com.cn/finalpage/2026-08-22/1225493335.PDF`，SHA256 `308eb291405f9c3c9bb1c784ef80fa89e996fe307f9dd4d1b8e3a9ccba8a852b`；本地归档与独立核验在 `workspace/negative-minority-20260910/`。当前仍拒绝该账面桥接，未将负数归零、未增加估值成功计数；如引入有限责任或零索偿代理，须另层显式政策和义务边界，不能回写源事实。该单字段证据不是603029全输入PDF验收，也未证明其余46例原因相同。

## 五行业参考WACC对照验收

相同主库、资本参考、财务期及信息时点下，`a-share-damodaran-wacc-2026H1.json` 的真实报告为 `workspace/industry-reference-wacc-20260910/batches/batch-iutsjjq3.json`：347家条件估值、324家输入/政策拒绝，其他分母状态保持不变。相比固定10%批次，41家从拒绝转为成功、1家从成功转为拒绝，306家共同成功；就绪度摘要相同。共同成功公司的标准事实请求、资本参考、除WACC外的经营政策、逐年收入/EBIT/再投资/FCFF及股权桥接分量逐项完全一致。

347份成功请求均从标准事实与参考原值以独立Decimal程序重建行业D/E权重、Beta/ERP/国家风险、信用档利差/WACC、十年DCF与每股值，全部通过，run ID集合与成功集合一致。五行业WACC分别约：Computers/Peripherals 8.0754%、Machinery 7.4227%、Furn/Home Furnishings 5.7531%、Beverage (Alcoholic) 4.9267%、Food Processing 4.2140%。这些含统一BBB档、中国风险暴露与25%税盾等政策，不能声称公司实际风险成本。安克同一机械经营预测从84.49046变为126.13785元/股，只是本次折现率对照，不替代专门研究结论。

324项拒绝为203收入/调整EBIT非正、67股权残值非正、47股本/现金/账面索偿符号、6金融业务及1茅台范围不匹配。茅台继续不适用普通合并模型，未因来源酒类分类获放行。002651是成功转拒绝的一例：十年预测FCFF全部负，WACC从10%降至7.4227%后，现金流出现值由−963.04降至−1,078.50百万元；终值现值增加不足抵消，股权由95.11转为−3.01百万元，独立复算与拒绝一致。低折现率不保证任意现金流序列的净值上升。原模型允许现金覆盖负经营价值的显式经营情景，不能据此声称已计入关停/战略选择权。

对照与独立复算脚本/结果在同目录 `compare_batches.py`、`comparison.json`、`independent_check.py`、`independent-final.json`，本地请求依赖和非PDF逐公司验收边界与上批一致。78行业展开工具的Python全套235通过、4项既有跳过；真实跨行业扩大批次仍待执行。

## 北交所代码切换解析接入

已有四份交易所原文现由可调用的 `internal/source/bse` 解析器处理，复用冻结Python执行边界和标准库HTMLParser；不新增依赖。输出248组原始旧/新代码、来源简称、原始挂牌日期字符串、切换日与来源表行，另保留四份文件的独立SHA和解析器摘要。原CSV校验入口复用生产解析器并继续锁定已归档哈希和逐字节CSV，避免两套提取逻辑漂移。

真实原文测试逐项匹配248行，篡改切换日期、删除特殊代码、把精选层挂牌脚注改成北交所上市日均拒绝；即使设置 `PYTHONOPTIMIZE=1` 也不能跳过生产校验。此阶段只有来源解析可调用，尚未发布数据库关系、放宽目录代码检查或增加标准财务事实。下一步须在关系发布中保留四份证据及共同可用时点，再让目录接收和公告身份解析共同验证；相同orgId本身不是跨代码放行依据。


上述北交所来源解析现进一步接入迁移037发布与公告身份解析，详见[证据、规则和验收边界](bse-code-transitions.md)。目录跨代码接收需官方关系、公告日、机构及唯一标准身份共同成立；物化会重新核验已解析的北交所公告。真实目录27条持久化回归和标准事实失效/恢复机制通过，主库244项修复尚未执行，不修改前文完成率。78行业真实批次仍在执行，最终全分母结果及独立估值复算待批次结束。


北交所代码关系现为日常刷新入口的固定身份准备阶段，置于财务同步之后、公告采集之前，使用当前Python解释器和绝对解析器路径发布到财务主库；不依赖可选的`--sync-references`。四原文取得或发布失败会使周期保留失败状态，仍核验既有可信发布及后续可用数据；失败文件不会替换已发布关系。阶段调度回归使用真实子进程控制退出码，不冒充新增在线源验收。

北交所身份补证已在主库副本完成真实补采、物化与幂等验收：同5,569个证券、同报告期/披露截止，核心输入齐全5,069→5,304（副本95.24%），235家改善、无原先齐全公司退化；标准事实2,023,155条，重放增改删0。22个机构身份缺失和两处新增同日同名报告歧义继续明确隔离，详见[副本验收](bse-code-transitions.md#财务主库副本验收2026-09-10)。当前长批次仍使用原库，副本结果尚未更新到原主库；这不是已完成估值公司数。

北交所已新增[显式行业代理路由](../valuation/docs/bse-industry-proxies.md)：同快照3,029家沪深双来源分类共现支持48个TDX类别，80个类别暂不映射；只用于XBSE，名称和时点共同校验，不写成Damodaran公司分类。副本118家命中政策、其中89家核心输入齐全；5,223家非北京股票路由不变。实际估值验收尚在执行，命中数不记作成功估值数。


北交所限定代理批次已实际完成：118家命中规则中57家条件股权估值、30家明确拒绝、31家缺输入，零执行异常。57份全部从归档标准请求独立Decimal复算WACC、逐年DCF及股权桥接通过，其中50家为本轮新增核心输入齐全公司。结果与拒绝分类见[实际验收](../valuation/docs/bse-industry-proxies.md#副本实际批次验收)；不是57家公司逐份PDF验收或市场价格结论，原主库尚未更新为副本。

新增[安徽凤凰32项模型输入原文核验](../internal/ingest/testdata/bse-valuation-2026/README.md)：27项数字位级匹配，5项空白/横杠明确保留边界；现金及等价物与含保证金的货币资金分别核对，股数使用股份结构表。六期TDX切片、四PDF、标准输入和台账进入离线Go/Python回归，无新依赖，不把原文空白改成数值零。
