# 真实财务样本可重复验收

执行日期：2026-09-05；代码：`03eca37`。本次将此前工作目录中的真实证据固化为离线回归，在新建临时 DuckDB 中重新执行采集、身份解析、事实写入、公告关联与物化。全部通过，未发现需要修改生产代码的新问题。

## 结果

| 检查 | 本次结果 |
| --- | --- |
| 独立财务数值 | 四家公司 × 四个字段，共 16 个第四季度金额与 TDX float32 原始位一致，包含负利润、负现金流。 |
| 目录完整性 | 原始响应声明 `totalpages=2`，生产客户端实际读取三页，得到全部 12 条公告。 |
| 失败恢复 | 第二页注入 HTTP 503，第一页面的五条公告保留、完成检查点为零；关闭并重新打开数据库后恢复至 12 条，失败运行记录保留。 |
| 正文 | 六份 PDF 的大小及 SHA-256 一致；从元数据模式切换到正文模式仍读取三页并归档六份文档；强制重扫复用全部正文。 |
| 目录重放 | 完成窗口跳过，HTTP 请求为零。 |
| 财务写入 | 六家公司 × 584 字段，共 3,504 条源事实；重放新增、重新归属、删除均为零。 |
| 关联与物化 | 六条源版本关联，54 条标准事实；待解析财务身份、待解析公告、待关联、歧义关联及拒绝物化均为零。 |
| 期间与单位 | 48 条金额为 Q4/CNY，六条期末股本为 FY/share；其中 16 个金额同时核对公告 ID 与原文。 |
| PIT 边界 | UTC 2026-03-06 15:59:59 查不到事实，16:00:00 可见 54 条，即中国 3 月 7 日零点。 |
| 物化重放 | 新增、更新、删除、拒绝均为零，仍为 54 条。 |
| 更正版本规则 | 已有 `TestCNINFOToPointInTimeFundamentalEndToEnd` 通过；此项使用模拟原始/更正版本，不是本次真实归档的历史版本证明。 |

例如德赛西威 FN230 对应年报第四季度营业收入 10,220,627,011.05 元，TDX 编码值为 10,220,626,944 元；落库期间必须为 Q4。校验没有用宽松误差掩盖季度/全年口径错误，也不宣称恢复源 float32 丢失的小数。

## 重复执行

仓库根目录执行，无网络、无旧数据库依赖：

```bash
go test ./internal/ingest -run '^TestReal(AnnualReportValues|FinancialWorkflow)$' -count=1 -v
```

同时验收本地真实 PDF：

```bash
ALPHALAKE_ACCEPTANCE_RAW="$PWD/workspace/live-sync-20260905/raw" \
  go test ./internal/ingest -run '^TestRealFinancialWorkflow$' -count=1 -v
```

未设置变量时明确输出未校验正文；设置后缺失或损坏归档会失败。本次两种模式均实际运行通过。`go test ./...`、`go build ./cmd/alphalake` 及提交前的 `git diff --cached --check` 通过，无新增依赖。

样本、原文定位、校验和及裁剪说明见 [样本说明](../internal/ingest/testdata/annual-2025/README.md)，逐项金额见 [values.csv](../internal/ingest/testdata/annual-2025/values.csv)。小型真实响应与裁剪财务记录随代码提交；完整 PDF 仍在本地归档中。数值由原文人工核对并固化，没有引入通用 PDF 解析器。

## 边界与下一步

本次没有重新访问上游，也未重新验收行情、公司行动、全量财务包下载或全市场覆盖。HTTP 服务只重放原始目录与 PDF 字节；TDX 使用真实记录切片，故不创建完整上游包的完成检查点。失败恢复验证的是请求失败后的持久化续跑，不是进程强杀或断电测试。

当前归档只有九月观察到的财务版本；PIT 时间边界正确不等于证明三月当时的全部数值版本相同。主数据切片也不能证明官方历史上市沿革。

这六家样本没有身份或关联缺口，尚不足以将权威生命周期判定为当前主要瓶颈。下一步应扩充不同季度、真实更正及历史代码变更样本，先统计待解析/歧义/拒绝原因；按实际缺口补生命周期或字段映射。保留通用 PDF/XBRL 自动提取为后续工作。

## 追加：Q1–Q3 真实样本验收

追加代码：`3c5f843`，执行日期同为 2026-09-05。本轮新取得大为股份、德赛西威的六份 2025 年季度/半年报、两个真实目录响应及三个完整 TDX 财务包，再将裁剪样本交给生产解析和物化函数离线回归。没有发现新的生产代码问题。

| 检查 | 结果 |
| --- | --- |
| 新增独立金额 | 两家公司 × 三个季度 × 四个字段，共 24 个；加上首轮 Q4 的 16 个，累计 40 个。 |
| 季度与累计口径 | 年报明确列示的单季度值精确累加后，与当期 Q1/H1/9M 报告累计值全部一致；Q2 用 H1−Q1，Q3 用 9M−H1 交叉核对。 |
| TDX 原始值 | 三个完整包均通过当次清单大小及 MD5 核验；24 个金额与对应季度源记录的 float32 位逐一一致。 |
| 原始证据 | CSV 保留 PDF URL、物理页码及 SHA-256；每个裁剪 ZIP 仅两条完整记录，共 6,854 字节，字段内容不变。 |
| 本地物化 | 八条公告含两份摘要；八条身份均恢复解析，六条源版本关联，54 条标准事实；无待解析、待关联、歧义或拒绝物化。 |
| PIT | 24 个金额分别核对原始季度/半年报公告 ID、单位、期间及中国次日零点边界；前一秒不可见，到边界值正确。年报只作数值复核。 |
| 重放 | 标准事实新增、更新、删除、拒绝均为零，仍为 54 条。 |
| 反向检查 | 临时将一个 Q2 样本标为 H1，以及将 Q2 金额换成半年累计值，分别被物化检查和精确累计检查拒绝；随后恢复原始样本。 |

本轮 `go test ./...`、`go build ./cmd/alphalake`、`go vet ./...`、提交前差异检查均通过。季度测试不访问网络、不依赖本地 PDF，复跑命令为：

```bash
go test ./internal/ingest -run '^TestRealQuarter' -count=1 -v
```

来源及金额推导见[季度样本说明](../internal/ingest/testdata/quarters-2025/README.md)。完整包、PDF、提取文本、下载元数据及两次预期失败日志保留于 `workspace/quarter-acceptance-20260905/`。PDF 原文在建立样本时通过已有 pypdf 工具提取并核对；普通回归不重新解析 PDF。

这补齐了季度口径的真实证据，仍未验证真实更正前后两个源版本、全市场历史身份或当前上游完整采集链路。下一项优先寻找能同时取得更正前后证据的真实样本，缺失旧版本时明确记录缺口。

## 核心字段扩充后复验（2026-09-06）

新增 20 个字段、35 个真实金额和两份仓库内完整 PDF，见[样本说明](../internal/ingest/testdata/core-financial-2025/README.md)。先前的 54 条标准事实是当时九字段版本的结果；扩充后年度六公司重放生成 174 条（48 条 Q4、6 条股数、96 条期末金额、24 条年度累计金额），季度六公司/报告期组合生成 162 条，另有 12 个零值折旧摊销候选因缺失歧义被拒绝。既有 40 个独立季度金额断言仍通过。

真实 `filings.duckdb` 的副本在 `workspace/core-financial-20260906/acceptance.duckdb` 升至 v18：首次本地物化新增 120 条、更新 54 条、删除 0 条、拒绝 0 条；重放新增/更新/删除均为 0。原库未修改。全套 Go 测试、构建、vet、两份 PDF 的 35 个金额重提取和错误 9M→Q3 的反向测试均通过。Go 依赖无变更。此轮没有完成全市场或多年度估值覆盖。

## 税项与债务扩充后复验（2026-09-06）

标准字段增至 38 个，新增 9 个字段的 16 个金额和一份璞泰来年报，另复用前一批两份完整 PDF。原文重提取、float32 编码、万元乘数、累计期间和 PIT 边界均通过；移除乘数的反向测试按预期失败，非法倍率会撤销失去依据的标准事实，恢复后可重建。

年度六公司样本现有 227 条标准事实（108 条期末金额、65 条年度累计金额、48 条 Q4 金额、6 条股数），1 个零值使用权资产折旧候选拒绝物化。季度样本现有 212 条事实、16 个歧义零值候选，合格事实重放无变更。更正样本现有 34 条事实、4 个歧义零值候选；旧包缺口未改变。

真实数据库的 v18 副本 `workspace/tax-debt-20260906/acceptance.duckdb` 升至 v19：新增 53 条、更新 174 条、删除 0 条、拒绝 1 条；重放新增/更新/删除均为 0，歧义候选继续保留诊断。原库未修改。`go test ./...`、构建与 vet 均通过，Go 依赖无变更。详见[税项与债务验收样本](../internal/ingest/testdata/tax-debt-2025/README.md)。


## 年度与 TTM 补充验收（2026-09-06）

迁移 020 新增 `fundamental.annual_asof` 和 `fundamental.ttm_asof`，先按公告时点选择事实版本，再组合四季流量、累计差额或期末存量。缺必要输入返回 NULL、缺失期间及对应输入血缘。具体规则见 [ADR 011](decisions/011-annual-and-ttm-windows.md)。

新增 2026Q1/H1 两份真实包、两家公司同期目录及七份完整 PDF，结合已有样本覆盖 002213/002920 的 2025Q1–2026Q2 连续六季度。新裁剪包四条记录与完整包逐字节一致。12 个公司/报告期记录建立 12 条公告关联，物化 432 条标准事实，24 个歧义零值候选拒绝物化；重放新增、更新、删除均为 0。

独立从 PDF 重提取 38 个金额，验证新季度 float32 位模式；24 组年度/跨年 TTM 四季加总与财报累计差额复算一致，误差界限来自源 float32 编码精度。另验证税项、资本开支累计路径、期末现金及半年报公告可用前后一秒。完整窗口数：2025Q1/Q2/Q3 各 38/76、2025 年末 76/76、2026Q1 68/76、2026H1 76/76。该分母只包含两家样本的 38 个已映射字段，不代表全市场或完整 DCF 覆盖。

合成回归实际验证未来更正、缺中间季度、存量不沿用、单位不匹配及非法期末。删除缺期空值保护后，测试因三季部分和 90 被错误返回而失败，恢复后通过。测试文件采用 `financial_window_test.go`，避免 `_windows_test.go` 被 Go 识别为 Windows 平台限定而在 Linux 跳过。全套 `go test ./...`、构建、vet、`git diff --check` 及离线 PDF 校验均通过，Go 依赖无变化。

本地交付位于 `workspace/ttm-20260906/`：`ttm.duckdb` 为两家公司六季度验收库，`annual-inputs.csv` 和 `ttm-inputs.csv` 分别为 2025 年度与截至 2026 半年末的输入表，各 76 行并附来源。原 v19 验收库的副本 `upgrade.duckdb` 已迁移至 v20，227 条标准事实整行有序摘要升级前后相同，原库未改动；该年度包样本缺少前三季，因此年度宏如实返回 179 个完整窗口、49 个缺输入窗口。运行日志和 CSV 是本地交付，仓库内回归与 PDF 校验不依赖它们。

下一步以一家普通非金融公司为样本，补充附注中的 EBIT 调整、到期债务拆分和非现金营运资本，形成可复算的估值财务输入；旧更正包和诊断去重等边界事项仍保留在案。


## 安克创新财务输入补充验收（2026-09-06）

以安克创新（300866）的 2025 年报、2025/2026 半年报建立三张分析表：融资及投资调整后的 EBIT、含一年内到期拆分的账面有息债务，以及非现金营运资本。详见[研究记录](anker-valuation-20260906.md)和[离线样本](../internal/ingest/testdata/anker-valuation-2026/README.md)。

三份完整原文及目录强制归档，314 个金额通过指定合并报表/附注、物理页、项目和本期/比较列重新提取。三份完整源包分别裁剪一条 300866 记录，字节与全量原件一致；已有 23 个映射共 69 个值按 float32 位核对，FN439 继续按万元编码。Go 测试不触网，无正文环境变量降级。未新增映射、数据库迁移、Go 依赖或生产物化路径。

分析脚本以 Decimal 使用 PDF 原文金额，分别复算利润表、财务费用、投资收益、公允价值损益、债务流动/非流动拆分和营运资本上下两种算法，三张 CSV 保留分量、公式、状态与公告 ASOF。债务反向测试将流动租赁系数改为零，与附注总额的独立恒等式随即失败；正确脚本只读重放逐字核对三张 CSV。全套 Go 测试、构建、vet、差异格式检查及离线 Python 验收通过。

2025 年度/截至 2026 半年末 TTM 的政策定义 EBIT 分别为 2,640,929,058.51 元、3,230,457,170.03 元；2026 半年末账面有息债务为 3,066,915,772.71 元。基准保留外汇相关损益，敏感性单独列示，不假定所有非经常性损益均应删除。

历史“其他应付款”等混合项未完全拆分，完全分类后的经营营运资本年度/TTM 变动明确为空；宽口径变动可复算，但没有冒充完整再投资现金流。暂定分类、研发资本化、可转债市场价值和租赁/资本开支衔接等限制均记录在研究说明。

上轮 review 的两个 minor 继续在案：既有真实 TTM 测试的 float64 精确比较仍依赖固定样本；`ttm_asof` 不为完全无标准事实的证券产出行，全市场覆盖率仍需独立证券分母。本轮分析结果使用 Decimal，但没有声称已修复这两项。


## 安克现金流与再投资衔接补充验收（2026-09-06）

复用上一轮三份原始 PDF 和三份裁剪源包，新增 95 个原文金额及 15 个源值，总计 409/84。新源值为 FN114/136/137/138/581 的三期金额；FN581 按万元编码，投资性房地产折旧另留 PDF 证据，不混入 FN136 或猜测新映射。新增 `cashflow.csv` 与 `reinvestment.csv`，原三张分析表数值未变。

三期现金流补充资料 19 个有符号分量均精确加总到主表经营现金流，并与主表流入减流出一致；资本开支与投资活动流出小计核对，始终限定合并表。2026 半年报 29 个现金流比较列金额与 2025 半年报原披露一致。存货原值减准备与净额相符，存货原值期间变动与补充资料中的存货增加逐分一致，剩余营运口径差额单列。

TTM 现金资本开支 319,634,286.33 元，披露折旧摊销合计 258,934,931.32 元，经营现金流 2,335,556,990.84 元。TTM 现金流营运净占用与上轮宽口径余额变动之差仅 -495,700.62 元，但内部有存货准备增加 206,180,172.86 元与其他未归因差额 -206,675,873.48 元抵消；不能将小净差额解读为分类已完整。估值 FCFF 和完全分类的经营营运变动继续留空。

漏掉使用权折旧的反向测试在净利润至经营现金流的恒等式处失败；正确脚本在新建 Python 3.12 虚拟环境、安装 `pypdf==6.17.0` 后，以默认命令重提取并逐字核对五张 CSV 通过。Go 相关回归、全套测试、构建、vet、差异格式检查通过，无生产代码、迁移或 Go 依赖变更。

`.github/workflows/ci.yml` 已增加固定 Python/依赖版本和强制离线验收步骤。依赖安装需联网，金额提取和 CSV 校验只使用仓库证据；本轮本地等效命令通过，未声称已运行远端 GitHub Actions。上轮 EBIT “独立起算”的注释和说明已改为同一组数据上的代数一致性校验，不宣称两条独立证据链。
