# 达摩达兰方法核对：净再投资与终值

本轮检查普通非金融FCFF路径：标准窗口→`build_book_dcf_inputs`→`compute_dcf`，历史增长与零增长显式政策共用该路径。结论是已检查的净再投资与终值公式一致，测试的一处弱断言已补强；另修正WACC代理分支错误继承的市场资本结构标签；不代表所有估值模型、公司WACC或全部默认参数完成核对。

当前通用路径进一步核对见[2026-09-12闭环验收](../valuation/research/method-closure-20260912/README.md)：补查实际M1—M6接入范围，并修复M4缺期初资本仍产生隐含ROIC的问题；新增统一结构化缺口披露。下文为历次修复的当时范围与结果，“报告不变”不包含本次有意修正的ROIC诊断字段。

方法依据：[达摩达兰增长与再投资说明](https://pages.stern.nyu.edu/~adamodar/New_Home_Page/invfables/growthdeterminants.htm)、[终值中的超额回报](https://pages.stern.nyu.edu/~adamodar/New_Home_Page/valquestions/termvalueexreturns.htm)。增长、再投资与资本回报应联动，终值需要为稳定增长扣除相应再投资；是否存在超额回报决定增长对价值的影响。方法依据与公司预测实证分别记录。

| 检查项 | 当前普通FCFF实现 | 结论与边界 |
| --- | --- | --- |
| 净再投资 | 收入增量/收入资本比率；普通政策显式采用零年滞后 | 是净资本开支加经营营运资本投入的预测代理，不是现金购建支出 |
| FCFF | NOPAT−净再投资 | 未再额外扣除全部现金资本开支，避免将净额与毛额混用 |
| 终值投入 | 下一年NOPAT×g/ROIC | 已扣除增长对应的投入；g=0时净投入为0 |
| 终值折现 | 下一年NOPAT×(1−g/ROIC)/(WACC−g) | AlphaLake政策校验g<WACC及g<ROIC；现金流从预测期末再折现 |
| 无超额回报 | ROIC=WACC时，终值/下一年NOPAT=1/WACC | 新回归固定十年预测，跨三个g值检查这一关系；不是在改变下一年NOPAT时声称绝对价值完全不变 |
| 增长上限 | 共享引擎非显式覆盖路径以无风险利率限制终值增长 | 原测试仅检查正值，已改为完整结果与上限情景相等；低增长超额回报情景作对照 |
| 显式终值覆盖 | 普通政策显式给定g，绕过共享引擎默认上限 | 不代表g已获得经济长期增长依据；需要逐政策审阅币种、名义/实际及长期增长假设 |

安克两份普通政策的2%终值增长、10%WACC与10%终值ROIC仍为显式参数。零增长研究只支持两年/三年收入预测，不能用该研究替代终值参数依据。固定WACC对照也不等于公司市场WACC估计已完成。

回归检查了g=0、2%、4%，ROIC=WACC及ROIC>WACC两种情况。零增长的十年预测净再投资为0、FCFF为NOPAT，同时原输入仍保留正的历史现金资本开支和折旧；这用于锁定口径边界，不宣称历史输入就是未来维持性投入。

## 继续核对的顺序

1. 终值增长、终值资本回报、终值风险的采用依据：先区分显式对照与有来源的公司政策，不将默认参数视为达摩达兰的公司推荐值。
2. 公司WACC的币种、无风险利率、行业业务权重、债务税盾及资本结构，遵循现有时点与参考来源契约。
3. 特殊模型及权益桥接：金融业务、研发和租赁调整、可转债、少数股权及非经营资产的范围一致性。

发现方法偏差时修正共享根因并对既有运行作对照。历史预测继续采用固定开发/留出设计，校准输入，不为提高回测指标另造估值方法。

## WACC来源标记修正

共享风险模块的`direct`、`industry_average`、`decile`分支并不按该公司的市场资本权重计算WACC，却继承了结果模型默认`capital_structure_basis=market_values`。现明确返回`not_used`，具体采用方式仍由`approach_used`区分；详细市场计算以及来源绑定的目标/行业/市场估计权重分支不变。非详细分支保留的权重和部分成本字段是兼容占位，不能当作独立估计分量，使用方须按`approach_used`解读。此次没有核验所有行业/分位参考的数据版本与可用性。

以安克两份已存标准请求复验：请求、生成输入及审计逐项相等，整份报告仅资本结构来源标签不同，原92.1504与42.2846元/股完全不变；不是主库重新采集或全市场估值重跑。引擎版本因代码修正而变化，新的run ID分别为`e54930fa9e04bccd435aa1e87a8fafcf269fcc987971e66f1a2a059c450debbe`和`1f1c3ce96978a704fb93f75ece99e4313f54ee7ba02addd31608b7b92f60e206`；新运行重放完全相同，旧运行文件不改写。详见[对照回执](acceptance/wacc-basis-audit-20260911.json)。

新回归覆盖三个代理分支在市值变化时仍使用各自WACC来源且不冒称市场权重，并确认详细分支仍计算债务权重。共享方法测试不依赖新PDF或外部网络，没有增加依赖。单纯公式测试不能替代此前的原文与历史预测实证。

最终验收：Python全套292通过、4项既有上游测试数据缺失跳过；Go全套及构建通过。两份真实标准请求的数值逐项对照与幂等重放通过，文档链接及差异检查通过，无新增依赖；没有Go源代码修改。

## 无效终值输入拒绝

共享`compute_dcf`已移除无效Gordon分母时返回零终值的分支。在预测前要求终值WACC、增长均为有限数，且WACC严格大于增长；不成立时抛出专用`InvalidTerminalValue`（继承ValueError）。这只修正数学适用边界，不新增增长预测公式；当时未覆盖ROIC范围；下方终值ROIC补验现已补足有限正值边界，其他无效输入仍需分别检查。

普通AlphaLake API沿用既有422结构；通用创建、覆盖和其他未拦截该异常的API入口由统一异常处理器返回422。回归实际执行通用创建与覆盖：无效创建不产生会话、无效覆盖不改原会话。通用敏感性入口既有失败单元格处理继续保留，未将失败情景计为成功结果。

两份已存安克标准请求的新运行与修正前`request/inputs/audit/report/status`逐项完全一致，仅引擎版本和run ID更新。原92.1504及零增长42.2846元/股不变，完整原始运行哈希和新ID见[终值拒绝对照回执](acceptance/terminal-guard-20260911.json)。原运行保持不可变，不是一次新数据采集或估值价格更新。

原零增长研究选择绑定旧引擎文件哈希；当前重放用临时当前选择并完整比较历史结果，提交的原选择和结果不改写。重放仍为已公开样本的兼容性检查，不算新留出。无新增依赖，API测试使用现有dev依赖httpx；本机测试客户端产生弃用提示，未为消除提示更换依赖。

终值拒绝最终验收：Python全套297通过、4项既有上游测试数据缺失跳过；Go全套及构建通过，真实请求同值重放、API失败会话保护、完整历史研究数值重放、文档链接与`git diff --check`通过。


## 终值ROIC边界补验

在三年收入预测复验失败后，回到共享再投资链核对发现：`compute_dcf`原先对零、负数、NaN的终值ROIC使用零再投资率，无穷大也会令`g/ROIC`为零。通用`ValuationAssumptions`可接收这些值，普通AlphaLake政策的更严约束不能保护所有通用调用。

依据上方达摩达兰稳定增长再投资关系，当前稳定经营FCFF模型需要有限正值的终值资本回报；缺失/无效回报不能替换为零投入。现于解析终值ROIC后统一检查有限且严格为正，失败抛出既有`InvalidTerminalValue`，并移除零再投资率回退。未显式提供ROIC时仍沿用既有`ROIC=WACC`的无超额回报假设，并检查其解析后数值；这不是新增公司事实。未新增`ROIC>WACC`或`ROIC>g`的通用限制，不把价值毁损情景自动排除；零/负回报业务需要另行明确模型与经济解释，本模型直接拒绝。

修改前新增回归的四项全部失败（没有抛出异常），修改后通过；API回归同时覆盖原WACC错误和零ROIC错误，返回422、无效新请求不新增会话、无效覆盖保持原会话。已有零增长恒等式及NVIDIA Ginzu等回归继续检验有效情景；本次不改变负终值增长的既有处理或S/C默认政策，不能称全部再投资边界完成。

两份已存安克标准请求重放：`request/inputs/audit/report/status`逐项完全相同，92.1504与42.2846元/股不变，第二次重放幂等；旧文件哈希不变。新引擎版本及run ID见[补验回执](acceptance/terminal-roic-guard-20260911.json)，新运行保存在独立workspace目录，不是重新采集财务或全市场重估。这里证明有效请求未受改动影响，不是预测准确度提高的统计证据。


全套首轮曾因半量研究的原引擎哈希绑定失败（324通过、1失败、4跳过），保护按预期生效；测试改为同时断言原CLI拒绝新引擎，并从原源快照用当前代码重建全部开发/留出结果，再与归档逐项比较。旧研究协议、源快照与结果不修改，不将保护关闭当作修复。该兼容性检查不证明新预测规则有效。

终值ROIC最终验收：Python全套325通过、4项既有外部TEST_DATA缺失跳过；Go全套测试和构建通过，真实请求同值重放、历史结果完整兼容性比较及文档差异检查通过。新检查沿用既有Python CI路径，无新增依赖或环境变量，无Go代码修改。

## 研发重分类的税项与期初资本

已修正共享引擎的本期研发重分类。依据[达摩达兰无形资产说明](https://pages.stern.nyu.edu/~adamodar/New_Home_Page/littlebook/intangiblevaluedriver.htm)：同一税项假设下，研发资本化对税后经营收益与净再投资作同额调整，纯重分类不改变历史现金流；回报率及增长则可能变化，不能把重分类当作现金创造。

旧M3对调整后EBIT全额乘税后系数，导致研发净加回60、税率20%时FCFF少12；净加回为负则反向虚增。新共享函数 `after_tax_operating_income` 保留原税项：先从M1的EBIT总调整中排除租赁调整，取研发净加回D；NOPAT＝调整后EBIT×(1−t)＋D×t。M3、公司ROIC与M5复用同一公式，EV/EBITDA也从同一NOPAT扣再投资得到分子，不再混用原始EBITDA推回的另一套经营利润。

这延续M1已有税项不变的分析重分类，不断言中国或境外税法允许全部当期扣除，不新增加计扣除、亏损抵税或递延税资产。M4未来利润率、税率及再投资仍是独立预测假设，本次未把历史重分类等同于未来研发投入预测。

本期M3期初研究资产改为“期末资产－当期新增＋当期摊销”；公司边际收入/资本比也扣回当期研发净增加，避免两期资本使用同一期末研发资产、使研发投入被抵消。M3本期ROIC使用期初资本，公司比较面板ROIC仍使用期末资本，分母定义保持区别。多年历史诊断随后已按下节重建逐年研发资产，不再复用当前资产；期初租赁PV仍有代理，完整历史经营资本和旧期源语义未闭合，不能将已修研发算法作为后续增长规则的有效性证据。

新增回归覆盖研发扩张/收缩、同时有/无租赁调整、FCFF/FCFE不变、期初研究资产、公司边际资本以及两种倍数。旧实现四例均在现金流不变断言失败；税项修正后、期初资本修正前四例又在期初资本断言失败。另将安克已有2020–2025研发CSV与三档税项不变情景接入共享函数，期初/期末资产与NOPAT吻合；原情景税额按分取整，测试保留该税额，不把重新按名义税率计算出的不足一分差异当成模型错误。真实样本FCFF仍为空。

[两份真实标准请求重放](acceptance/rd-tax-guard-20260911.json)的请求、输入、审计和完整报告均不变，重复执行相同；仅引擎版本及run ID更新。原文件经SHA核对且未覆盖，结果另存 `workspace/rd-tax-audit-20260911/runs/`，不是主库财务更新或全市场重新估值。

研发修正验收：Python全套333通过、4项既有外部测试数据缺失跳过；Go全套测试和构建通过。安克原文验证器重新提取四份年报180个历史金额及7个资本开支金额通过；新增回归进入既有Python CI目录，零新增依赖。原文重提取、情景复算和合成反例各有验证范围，均不等于研发预测有效性。文档链接、JSON及差异检查通过。


## 逐年研发资产与历史回报

历史诊断现在按各年度及此前的连续年度研发支出，复用M1直线摊销方法重建期末研究资产，并用同一税项不变函数计算年度NOPAT。历史利润率采用相应的研发调整后EBIT，ROIC的分母采用上一年末资本；不读取当前调整输入中的研发资产或当前研发金额代理过去。

期末研究资产只需要n个年度投入，因为第n个过往年度期末权重已为零；当年摊销需要当前加前n年，即n+1个年度。资产可算而摊销不足时，只保留资产相关资本比率，利润率及ROIC留空。缺失、负数、非有限研发、重复年份或非连续年份阻断相应窗口；显式原始财务输入零允许参与，但适配器仍不得把未解析的TDX零值当作已知零费用。

研发启用但年度诊断不完整时，编排报告增加明确提示。没有将缺研发的年度退回未调整利润/资本后混算平均值；收入增长仍独立计算。现有平均值仍是可用年度的平均，逐年空值必须一并查看，不能据平均值存在宣称三年/五年窗口完整。历史税率现仅使用该年有效税费/税前利润，已移除21%缺失代理，详见下方历史税项边界；历史经营资本分类和租赁调整仍未完成，不能把这组诊断称为完整经济ROIC。

回归以三年、五年两种寿命从Decimal年度投入独立重建资产、摊销和回报；旧实现两例均在ROIC数值处失败。新实现覆盖最早摊销年度不足、缺项、重复年份、负数/NaN/无穷值、显式零；把最新年度研发提高到一百万不改变任何更早年度的ROIC、资本比率或利润率。小型编排样本锁定缺项提示及不静默退回未调整利润率。这是计算与时间依赖检验，不是研发增长预测的统计复验。

[本轮两份标准请求重放](acceptance/historical-rd-guard-20260911.json)完整请求、输入、审计、报告与状态同值，重复执行一致；旧文件未修改，新运行位于 `workspace/historical-rd-audit-20260911/runs/`。这两份请求未启用研发资本化，只验证已有路径兼容性，不替代启用研发的年度边界回归。

逐年研发验收：Python全套335通过、4项既有外部数据缺失跳过；Go全套测试和构建通过。新增回归沿用既有Python CI，无新依赖、迁移或Go修改；文档链接、JSON与差异检查通过。未运行新的增长预测候选或宣称DCF准确度改善。

## 历史增长与回报的年度间隔

继续追踪再投资输入时发现：共享M3历史诊断在研发关闭时按列表相邻项计算一年收入增长及期初资本ROIC，没有确认财年连续。2024收入1,210与2022收入1,000被算作21%“一年增长”，并借用2022年资本作为2024期初资本；三年CAGR也按第4条记录取值，可能实际跨四年。

现对逐年收入增长与ROIC要求当前年及上一年身份唯一、相邻记录确实相隔一年；缺年、重复或逆序的相关结果留空。三/五/十年CAGR按实际财年查唯一端点，允许缺中间年度（复合增长只需端点），不按列表条数替代经过年数。年度利润率和期末资本比不受这一跨年条件影响；已有可用年度平均值口径不改，不因此宣称平均窗口齐全。

新增反例在旧实现上失败、修正后通过，同时覆盖缺年、重复及逆序；研发历史仍有其更严格的连续摊销要求。本次修正历史诊断，不改变当前预测增长政策，也不更改此前独立研究工具按报告期取数的结果。税项、资本分类与租赁的已有边界仍保留。

[两份真实标准请求](acceptance/calendar-guard-20260911.json)的请求、输入、审计、完整报告及状态均与此前一致，幂等重放通过；92.1504与42.2846元条件值不变。新运行独立保存在workspace/calendar-guard-20260911/runs，旧运行哈希不变。它们没有缺年反例，只验证兼容性；不能把同值重放当作预测有效性证据。

年度间隔验收：Python全套338通过、4项既有外部测试数据缺失跳过、7条警告；Go全套和构建通过。新增回归进入既有Python CI路径，无新增依赖、迁移或Go修改，文档链接与差异检查通过。

## 当前回报的期初资本窗口

[权益与现金研究](../valuation/research/tdx-capital-inputs/book-capital.md)后续调用链核对发现，前述年度间隔修正仅覆盖历史序列；`compute_cashflow_and_growth`当前ROIC/ROE分支仍接受任意`raw_prior_year`。相同2024年利润与资本金额下，传入2022年、同年或未来资本，都产生相同ROIC及基本面增长率。编排器另将普通季度旋转后的LTM与年度期初资本混用，年度标签本身不能证明季度窗口对齐。

现于共享M3当前回报分支要求期初记录恰为上一财年；若同时提供历史序列，则当前及上一年都必须唯一。缺年、同年、未来年或重复年度，期初资本、ROIC、ROE及依赖它们的基本面增长率留空；可独立计算的再投资与FCFF/FCFE不因缺资本而删除。显式负资本的原有处理不变，未新增现金、债务或税项分类政策。

普通编排器在`quarters_since_10k>0`时不再传年度期初资本。LTM对应的期初资本尚无明确对齐输入，当前回报留空并提示`Current return metrics unavailable: aligned opening capital required`。已有`prepared_ttm`路径本来就不传年度期初资本，本次保留该行为；没有把上一年年末或列表中的某条季度记录猜作期初。共享原始财务类型只含年度标签，直接调用者仍须提供口径一致的年度输入；这不是对所有外部季度数据建立了完整时点模型。

六个新增回归在旧实现全部失败，修复后通过，覆盖三类错误年份、两类重复年度和季度LTM；同时锁定正常年度仍有ROIC、缺资本不抹去可算现金流。受影响的当前回报现在与历史序列保持期间约束一致，但完整经营资本及历史可比性仍须分别验证。

[两份已存安克标准请求](acceptance/current-capital-guard-20260911.json)的请求、输入、审计、完整报告、状态均与此前逐项相同，92.1504及42.2846元条件值不变；第二次重放幂等，旧文件哈希不变。新引擎/run ID记录在回执，新运行位于`workspace/current-capital-guard-20260911/runs`。样本未包含本次无效窗口，只证明正常请求兼容，不是预测有效性或全市场回归证据。

当前窗口验收：Python全套351通过、4项既有外部数据缺失跳过、7条警告；Go全套和构建通过。六项新回归进入既有Python CI；文档链接、旧运行哈希、重放与`git diff --check`通过。没有Go、依赖或迁移变更。

## 显式期与终值再投资的政策切换

固定五家公司基线的[再投资衔接诊断](acceptance/reinvestment-transition-20260911.json)已重放安克、苏泊尔两份既有标准请求，另外三家生产阻断保留。财务期2026H1、信息截止2026-09-10T04:42:23Z，使用当时行业机械政策；不是新估值或当前目标价。

| 项目 | 安克 | 苏泊尔 |
| --- | ---: | ---: |
| 显式期采用的行业资本效率 | 2.4835 | 2.2730 |
| 第十年净再投资/NOPAT | 11.0940% | 10.6607% |
| 终值年净再投资/NOPAT | 20.0000% | 20.0000% |
| 再投资率切换 | +8.9060个百分点 | +9.3393个百分点 |
| 第十年至终值年FCFF变化 | −8.2177% | −8.6628% |

两家公司第十年及终值年收入增速均为2%，利润率及25%税率相同。显式期用`Δ收入/资本效率`，终值用`下一年NOPAT×g/ROIC`，终值ROIC为显式10%假设。因此即使收入继续增长2%，FCFF仍在切换处下降约8%。公式核对成立，不将差异直接判为代码错误，也不把连续平滑当作达摩达兰的强制规定。

达摩达兰[终值与超额回报说明](https://pages.stern.nyu.edu/~adamodar/New_Home_Page/valquestions/termvalueexreturns.htm)要求稳定增长对应足够再投资；本项目终值公式已满足这项关系。但行业资本效率与10%终值ROIC分别采用，不证明公司为何在这时发生回报变化。下一项经济依据应解释增长、利润率、资本效率及回报的过渡，而非继续单独寻找收入同比统计量。

纯代数地，在零投资滞后、利润率及税率稳定、增长率均为g时，使`Δ收入/S`与终值投入一致需要`S=ROIC/[(1+g)×利润率×(1−税率)]`。两例分别约1.3776、1.2116；这只是同一终值约定下的匹配比率，不是估计出的公司资本效率，不应替换行业来源。类似地，`g/第十年再投资率`给出约18.03%、18.76%的等效回报，不是历史ROIC或实测新增投资回报；不能跨期资本分母直接混用。

本轮以Decimal复算第十年净再投资、终值现金流与终值金额，并检验上述匹配比率恒等式。原输入、报告、政策及run均未修改；没有新增代码、依赖或数据采集。复现（已有Python环境与原始运行归档）：

```bash
PYTHONPATH=valuation/backend workspace/anker-agent-adapter-20260906/venv/bin/python - <<'PYTHON'
from decimal import Decimal as D
import json, math
from tools.compare_valuations import load_run, replay
receipt = json.load(open('docs/acceptance/reinvestment-transition-20260911.json'))
for row in receipt['companies']:
    if 'run_id' not in row:
        continue
    run, evidence = load_run('valuation/backend/data/alphalake_runs', row['run_id'])
    assert evidence['sha256'] == row['source_run']['sha256']
    report, inputs = replay(run)
    dcf = report['dcf']; policy = run['request']['policy']
    nopat = D(str(dcf['ebit_projections'][-1]))*(1-D(str(policy['tax_rate'])))
    rate = D(str(policy['terminal_growth']))/D(str(policy['terminal_roic']))
    cash = nopat*(1+D(str(policy['terminal_growth'])))*(1-rate)
    assert math.isclose(float(cash), row['terminal_fcff_million_cny'], rel_tol=1e-12)
    assert math.isclose(float(D(str(dcf['reinvestment_projections'][-1]))/nopat), row['last_year_reinvestment_rate'], rel_tol=1e-12)
    assert report['final']['value_per_share'] == row['value_per_share']
print('two transitions reproduced; original valuations unchanged')
PYTHON
```

## 行业经营利润增长与收入增长分离

已修正继承的`fundgr_parser`语义：`Expected Growth in EBIT`原被写成`revenue_growth`，经`DamodaranStore.lookup_industry`进入行业收入增长参考，展示于输入页/增长对照及工作簿。现在解析为独立`expected_ebit_growth`，类型与查询保留该字段；缺乏真正收入增长参考时`revenue_growth`为空。工作簿单列“Industry expected EBIT growth (not revenue growth)”，输入页也单列行业EBIT增长，并移除收入增长参考来自fundgrEB的错误提示。

证据来自仓库继承的八份`fundgrEB*.xls`：表题明确为经营利润的基本面增长，E列表头为`Expected Growth in EBIT`。[证据回执](acceptance/fundgr-semantics-20260911.json)记录各文件哈希；全球表SHA为`d180bb311b2b9370f69ce51a5408a588d247e336bbd6078709b0c21093ad72dc`。八份表681个数值逐项与E列相等，列名被篡改为收入含义时明确拒绝；没有以缺字段为零。表内观察日期不等于发布日期，这批继承文件首次取得时间未知，不能借用另一份已同步参考的取得时间，也未新增AlphaLake参考库发布。

全球Computers/Peripherals的11.2215%、Furn/Home Furnishings的4.1255%现在仅作为行业经营利润增长参考；不自动用作公司收入增长、公司经营利润预测或已验证的长期增长。`ROC`和`Reinvestment Rate`源分量原样保留于解析层，本轮没有把它们接成公司事实或标准参考。

已追踪调用链：通用DCF收入路径由显式政策/共享历史规则生成，不直接以该行业字段填入收入预测。[两份安克/苏泊尔归档标准请求](acceptance/fundgr-semantics-20260911.json)经当前引擎完整报告精确重放，估值不变；输入类型只增加空的经营利润增长字段。已有归档run保留原内容，不能把它们的旧行业参考重新描述为已修正数据；新运行的引擎版本会变化。运行中的Web服务需要重新加载参考缓存，旧会话的来源不明收入增长值不做猜测性改名，需显式重新加载行业参考或重新建会话。

回归：`tests/test_fundgr_semantics.py`以真实地区表→解析→类型查询锁定字段分离，并用同长度二进制表头篡改检验拒绝；使用现有xlrd，进入现有pytest CI，无新增依赖或skip。前端`npm run build`未通过：当前与隔离的未修改HEAD副本均为相同25条TypeScript错误；本轮没有新增错误，但不将前端构建称为通过。

本轮完整验证：Python 379项通过、4项既有外部样本缺失跳过、7条既有警告（486.29秒），Go全套及构建通过。八表681项及两份真实运行/工作簿检查通过；前端失败及其基线提交、相同25条错误详见上述回执。无依赖变更。

## 历史税项缺失的回报边界

多起点利润误差分解后继续追踪资本效率调用链，发现共享M3历史诊断在税额缺失、税前利润缺失/非正时默认使用21%税率。缺税项的EBIT100、期初资本600会得到79/600的“历史ROIC”；该数值还会进入历史平均及前端`StoriesToNumbers`/`ClosedLoopStrip`的历史回报参照。它不是公司历史税项证据，不能无声用于多市场历史资本效率判断。

[达摩达兰的税率说明](https://pages.stern.nyu.edu/~adamodar/New_Home_Page/valquestions/taxrate.htm)区分财报有效税率与估值中的边际税率，并讨论亏损和研发重分类的税项影响。本次实现据此以及本项目的事实/政策分层约定，移除历史诊断的固定21%缺失代理；该页面并未要求所有缺项一律留空，留空是本项目在没有显式历史税项政策时的处理。

现在仅当该年税费、税前利润均存在且有限、税前利润大于零时计算历史有效税率。缺项及无法据此推算税率的年度，历史ROIC留空并退出可用年度平均/加权平均；明确零税费仍有效。收入增长、营业利润率和收入/资本比不因缺税额而消失。现有可用年度平均不代表完整三/五年窗口，继续连同逐年空值解释。未改税费正负号的既有源约定、当前NOPAT、预测税率、亏损结转或资本分类，不用当前边际税率填回历史事实。

六项新增回归在旧代码全部失败，修正后通过：税费缺失、税前利润缺失/零/负，以及非有限数值；同时保留明确零税费和非税诊断。原财年间隔回归改为明示21元税费/100元税前利润，不再借缺省税率证明其预期值。原研发三/五年队列及税项不变测试继续通过。

[两份真实标准请求](acceptance/historical-tax-boundary-20260911.json)只读完整重放不变：安克126.1378元、苏泊尔43.2526元，均为原条件结果，不是新目标价。归档历史预测回归仍要求原模型与实际核验结果一致；当前引擎代码版本变动产生的新`review_id`按内容重算，旧运行和旧复核归档不修改。含缺税历史的其他旧报告会改变历史诊断，不能据这两例宣称全报告普遍不变；需显式按新引擎重算并保留新的run ID，不回写旧归档。

这是计算输入边界修正，不是新的增长规则或预测有效性证明。完整历史经营资本、FCFF及多公司前瞻有效性仍待完成。

本轮验证：后端全套396通过、4项因既有外部TEST_DATA文件缺失跳过、7项依赖警告；Go全套和构建通过。六项回归在旧代码失败、修正后通过，已有pytest CI自动覆盖。无需新解释器或依赖，未改数据库、旧运行或研究评分。

## 资本效率输入边界

调用链为通用API/标准适配器→`run_full_valuation`→`compute_dcf`→`_reinvestment_path`。旧共享函数将资本效率为零或空直接返回零投入；负值反转投入方向，正无穷使投入为零，NaN传播。输入模型未排除这些显式数值，普通API亦可触发，不能把数据源已有校验当共享引擎的保障。

现于共享再投资函数统一要求两阶段的预测Sales/Capital有限且严格为正；无效值抛出`InvalidReinvestment`，通用API复用422结构化拒绝格式。原公式`Δ收入/Sales-to-Capital`不变；合法正比率下，收入下降的负净再投资及收入不变的零净再投资均保留。缺省比率仍采用原有2.5/继承规则，不把这次修复扩大为新公司资本效率估计。预测路径长度/滞后越界的既有防御逻辑不在本轮范围。

方法依据仍是达摩达兰[增长与再投资](https://pages.stern.nyu.edu/~adamodar/New_Home_Page/valquestions/growth.htm)：增长应与再投资数量及质量一致，负再投资也可能具有真实经济含义。**有限正分母是本项目收入增量预测分支的适用条件，不是所有企业历史投入资本必须为正的会计定律。**负投入资本、资本释放或效率改善须另外明确经济路径，不能用零/负预测资本效率替代这种解释；不删除对应公司的原始财务证据。

8项新拒绝断言在旧实现全部失败、修复后通过；另验证收入收缩的负投入。API真实调用验证零值修改返回422、原会话完整不变，以及负值新建拒绝且不留新会话。现有引擎测试141项通过，4项依赖外部TEST_DATA的原有案例skip，2条现有依赖弃用警告；新增测试进入既有pytest CI，不依赖原PDF。Go全套与构建通过，无依赖变更。

[真实回执](acceptance/capital-efficiency-input-20260911.json)保留固定五家公司分母：两份合法标准请求经现有`load_run`校验后完整重放，与旧报告逐项相同；另三家原阻断保留。安克126.1378元、苏泊尔43.2526元仍为原时点/原政策条件值，不是更新目标价。引擎版本改变，新生成运行的标识随之变化；未重写旧运行、未修改主库，不能把版本变化解释为财务或估值变化。

本地复验（项目后端虚拟环境；真实运行另依赖本地归档，不声称裸克隆CI覆盖）：

```bash
PYTHONPATH=valuation/backend workspace/anker-agent-adapter-20260906/venv/bin/python -m pytest valuation/backend/tests/engine -q
PYTHONPATH=valuation/backend workspace/anker-agent-adapter-20260906/venv/bin/python - <<'PY'
import json
from pathlib import Path
from tools.compare_valuations import load_run, replay
receipt = json.loads(Path('docs/acceptance/capital-efficiency-input-20260911.json').read_bytes())
for row in receipt['companies']:
    if 'run_id' not in row:
        continue
    run, evidence = load_run('valuation/backend/data/alphalake_runs', row['run_id'])
    assert evidence == row['saved_run']
    report, inputs = replay(run)
    assert report['final']['value_per_share'] == row['value_per_share']
print('two complete saved reports unchanged; three original blocks retained')
PY
```

## 增长—资本需求的联合输入诊断（2026-09-17）

通用账面/历史FCFF的`method_assessment.growth_capital_consistency`现从同一标准窗口读取FN304研发费用与FN114购建现金，并逐年并列增长、利润率、税率、NOPAT、净再投资、FCFF。缺分量留null，研发费用不再扣一次或直接转换为增长，购建现金不冒充完整净再投资。

依据[达摩达兰增长基本面](https://pages.stern.nyu.edu/~adamodar/New_Home_Page/valquestions/growth.htm)对新增投入回报、既有资产效率以及研发/租赁一致性的区分，新增模型诊断：以m=模型NOPAT/收入作`ΔNOPAT=Δ收入×m本期+收入上期×Δm`分解，交叉项归收入，属于代数分解而非独立因果证据。仅在收入增量和净投入均正时显示“收入贡献/净投入”代理，不把它改名为公司实际ROIC或此前缺分母的完整隐含ROIC。

资本释放、负FCFF及增长回报代理低于WACC分别提示可回收性、资金来源和经济依据审核；不把负值直接判错。行业历史平均采用为边际资本代理的声明保留，研发费用化与参考资本化口径尚未协调，公司资本效率verified仍为false。两份冻结标准请求的财务预测和条件每股值不变；相关回归核对收入/利润变化分解、零投入留空、负FCFF保留、缺研发字段不归零或改变原估值。未据此声称预测准确性提高。
