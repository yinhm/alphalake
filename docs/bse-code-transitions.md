# 北交所2025年股票代码切换来源

`sync-bse-code-transitions` 将官方代码对照、试点名单、试点上线日和其余股票上线日四份HTML分别归档，解析248组旧/新代码，再原子发布至迁移037的 `reference.security_code_transition`。该表是来源关系，不含推测的标准证券ID，不修改TDX或CNINFO原始代码。

```bash
alphalake sync-bse-code-transitions ./market.duckdb --python python3
alphalake sync-bse-code-transitions ./market.duckdb --offline --python python3
```

日常 `tools.refresh_valuate_alphalake` 已自动在财务同步后、公告采集前执行本命令，写入财务主库，无需另外启用参考数据刷新选项。

Python解析只需标准库，可用 `--parser` 指定绝对脚本路径。北交所下载显式发送 `User-Agent: AlphaLake/1.0` 和HTML接受类型；真实默认客户端曾得到403，相同公开地址带上述客户端标识可取得匹配已归档SHA的原文。失败运行保留，不将403视为空的完整快照。

四份证据复用已有 `data`、`publication`、`timing` 角色：对照表为data、试点名单为publication、两个上线通知为timing。每份保留独立原始归档及来源URL，发布可用时间/首次完整观测时间取四份文件首次取得时间的最大值。来源没有明确整体版本日期，`source_version` 保持NULL；切换日是关系字段，不能当作来源发布日期。

六只试点切换日为2025-05-06，另242只为2025-10-09；不能按后缀拼接新代码，835892→920992、837023→920123等例外保留。对照表的原始“上市日期”字符串单独保存，原精选层平移公司的该列是精选层挂牌日，不能据此生成北交所上市区间。

规范化摘要包含全部解析关系和四份来源摘要，因此只变辅助原文也会产生独立版本。发布核验全部原文身份、时间与角色；重放逐条比对关系，拒绝被改写的已发布解释。离线先固定最新发布版本，按其角色及URL读取同一组原文；损坏最新版本不回退旧版。任何失败或缺证据均不推进发布完成键。

真实独立库 `workspace/bse-publication-20260910/references.duckdb`：首次默认客户端运行1失败，明确客户端标识后运行2发布版本1、248条关系、4份原文；离线运行3仍为版本1、零新增。源解析及原子发布定向回归通过，覆盖脚注/日期/缺项、辅助文件单独更新、离线零请求、关系篡改、损坏最新证据拒绝及重开后的失败记录。全套Go测试、构建、vet及`git diff --check`通过；为控制4GiB机器的并发内存，检查期间暂停估值批次调度，检查后已恢复。

关系需要发布到财务主库，才能用于该库的公告身份解析。目录定向查询和本地公告重解析共用官方关系：机构ID必须匹配，旧/新代码须符合公告业务日，两个代码的全部已知TDX标识历史只能指向一个XBSE/CNY股票ID。优先使用公告日有效的原代码标识；缺少时，只接受官方切换边界另一端的唯一有效标识作为锚点。缺失锚点、重叠区间、不同证券ID或其他市场元数据冲突均拒绝，不制造历史TDX区间、不重写原始代码。

物化输出的 `filing_resolution_recovered` 只计本轮待解析→已解析，不把既有公告复核重复计为恢复。每次物化重新核验已解析的北交所公告及待解析公告：最新参考版本损坏时保守退回待解析，不回退旧版本；依据恢复后从保留证据重新解析。成功原因保留发布版本、来源行和公告日。最新关系版本不可校验时，本批北交所公告整体保留待解析；其他交易所不受影响。这里使用当前本地身份知识，不声称重建了各历史时点当时可获得的完整主数据。

真实目录回归已覆盖27条公告入库、分页重解析、证据篡改后退回待解析、恢复及重开后拒绝缺失证据；原代码、公告日期和保守可用时间均不变。另以一个明确合成的金额验证物化机制：标准事实1→0→1，源事实保留，恢复重放零新增。合成金额不用于这两家公司的财务正确性验收。

主库尚未发布并运行修复，244项既有公告失败未计为已修复。不能仅凭相同orgId放行其他代码，也不能把关系表或机制回归的存在当作全市场财务数据链已经闭合。

已补真实定向目录回归：试点833819→920819为3条切换前旧代码、13条切换后新代码；非试点837023→920123为5条旧、6条新。同公司orgId保持一致。2026年公布的2024年报更正用新代码，故代码生效判断必须使用公告业务日，不能使用报告期。样本没有证明旧代码可在切换后无限沿用，不能据此放宽为永久别名。两份完整原始响应、请求参数及SHA已入testdata，定向Go回归通过。

## 财务主库副本验收（2026-09-10）

为保持运行中的78行业批次输入一致，从无WAL且文件大小/修改时间稳定的主库建立 `workspace/auto-valuation-20260909/market-bse-validation.duckdb`。原库没有迁移或替换；副本复用同目录不可变原始归档。复制状态、运行日志及对比脚本保存在 `workspace/bse-publication-20260910/`。

副本运行4882发布248条关系和四份原文。使用副本实际TDX主数据，两份真实CNINFO目录共27条全部解析；本地346条北京标识均没有`valid_from`下界，该事实不被解释为完整上市生命周期，也未补造任何历史区间。

475个待补公告关联代码全部尝试，运行4883–5357新增400条公告、更新3,351条，184家公司有新公告；22个失败全部为机构查询返回零身份，没有猜测机构ID。补采接收的公告没有待解析项。这些是请求和归档计数，不证明475家历史报告期都已闭合。

运行5358重新解析3,664条公告，新增恢复9条、仍待解析435条；435为整个公告台账的混合分母，包含当前A股分母以外的公告，不能与缺输入公司数相加。其中北交所6条缺少唯一已知标准身份，继续保留证据。财务记录关联从32,045增至32,569，待关联945→419，歧义72→74。标准事实新增31,626条、撤除64条、更新0条，合计2,023,155条；源事实不因身份补证而改值。

同报告期2026-06-30、同披露截止2026-09-10T04:42:23Z、相同5,569个本地证券ID逐项比较：

| 财务就绪度 | 原库基线 | 已验收副本 |
| --- | ---: | ---: |
| 15项核心输入齐全，仍需估值政策 | 5,069 | 5,304 |
| 缺核心输入 | 469 | 235 |
| 无标准事实 | 30 | 29 |
| 源记录冲突 | 1 | 1 |

235家从缺核心输入变为齐全，另1家从无标准事实变为有事实但仍缺项；原有5,069家齐全者均保持齐全。副本核心输入覆盖率95.24%，不是财报全部字段覆盖率，也不是已审核估值比例。比较使用各库当前可核验的身份知识补齐历史公告，不声称这些身份依据在本地04:42时已经采集。

新增歧义分别涉及920273的2025H1和920445的2025年报：每处都出现同公司、同报告期、同日、同名的两份完整报告。直接与原库标准事实逐键比对，64条撤除全部属于920445的2025年报；不擅自选择较大公告ID，也不把同名报告视为内容相同。该公司在基线本就缺核心输入，没有造成齐全公司的退化。原文版本关系仍待后续核查。

运行5359重放：新增/更新/撤除全部0，新增恢复0，标准事实总数不变。首次物化耗时1分49秒、峰值RSS约1.62GiB；重放1分45秒、约1.87GiB，仅为本机本次测量，不是固定容量承诺。重放和扫描期间暂停批次调度，结束后已恢复。

后续更新：78行业批次已完成，2026-09-10T07:51:58Z将验收副本发布到原主库并保留备份。身份、参考与沪深实际估值输入精确比较无变化（仅物化运行编号/写入时间刷新）；重开主库后的就绪度JSON与副本完全一致。5,304核心齐全/95.24%现已是主库实测口径，仍不是估值产出率。详见[全市场验收](a-share-automation-acceptance-20260910.md)。
