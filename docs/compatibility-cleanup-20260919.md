# 当前契约与兼容清理

## 目标与边界

当前没有实际生产部署，不承担旧版本API、旧输入或旧库的长期运行兼容。只维护当前模型与契约，调用方同步修改；旧数据需要时显式转换或从证据重建。原始归档、审核与补充记录、冻结结果及上游credit不删除。历史复验绑定原提交，不要求当前程序承载全部旧契约。

这里清理的是版本兼容，不是证券历史身份、合法源格式差异或显式估值政策；来源别名和多市场语义不能仅因出现 `legacy`、`alias` 或 `fallback` 就删除。估值公式和经济假设变更另行说明。

## 调用链清单与进度

| 路径 | 实际使用情况 | 处置与状态 |
| --- | --- | --- |
| 源事实追加包装 `InsertProviderFinancialRecordsForArtifact` | 无调用方；导入已使用可撤除失效记录的 reconcile | 已删除包装，保留当前原子发布链 |
| 行业全成败包装 `IndustrySnapshots` | 无调用方；导入使用逐分类结果 | 已删除，保留分区失败诊断 |
| 日线只返回计数的 `SyncTDXDaily` | 仅一个测试调用；CLI使用完整运行摘要 | 已删除，测试转当前入口 |
| 字典式 `compute_ltm` | 仅一个测试调用 | 已删除，统一类型化财务输入；缺项传播测试保留 |
| 国家到地区旧帮助函数 | 展示函数无调用方，旧别名只返回US | 已删除；搜索保留既有默认US，估值仍按显式方法选择 |
| 证券主数据列表回退与无分区快照 | 多个采集接口及测试仍依赖，真实TDX已提供分区快照 | 待统一快照契约，保留缺失分区冻结和截断拒绝 |
| 不带运行ID的日线写入 | 测试使用；正常采集已关联运行 | 已迁移测试并删除无运行入口与双SQL路径；运行ID必须为正，拒绝无血缘写入与估值证据，观测和检查点仍原子发布 |
| 旧源事实身份回填 | 写入函数内仍执行迁移012的回填 | 待核对本地库后移除，不能删除正常身份修正与撤除逻辑 |
| v1估值输入迁移 | 多个测试及历史复验工具运行时转换冻结请求 | 待生成当前契约测试输入并移除常驻转换依赖，原冻结归档不改写 |
| 迁移001–046及旧版本测试 | 当前空库仍通过迁移链建库；历史专项发布工具依赖版本编号 | 待确定初始化基线及旧库一次性处置，同步工具与测试；不能仅删除SQL |
| WACC及指标兼容回退 | 引擎仍有旧参数/旧消费者回退 | 待逐调用方核对；涉及债务与资本数值，须区分兼容清理与方法修正 |

本轮不是全部清理完成。剩余路径必须继续处理；验收不能只证明删掉的函数没有引用。

## 本地数据盘点

只读检查 `workspace/auto-valuation-20260909/market.duckdb`：schema46、46条迁移记录；19,310,544条源事实中缺源证券代码0条；2,048,273条标准事实中legacy物化版本0条；该库日线0条。此检查只覆盖这一财务库，不能证明其他工作库不存在旧数据。数据库未修改；Python估值环境没有DuckDB模块，使用仓库已有Go驱动完成查询，未添加依赖。

## 最终验收要求

- 代码、测试、工具、skill、当前文档只使用当前接口，不通过旧转换器维持测试通过。
- 全新数据库建库、真实归档导入、标准事实物化与统一估值贯通；失败、缺项、原子发布、重放与血缘约束保持。
- 本地既有数据有明确去向；不可重建的审核、补充和政策留存，原证据及冻结结果未改写。
- 相同当前输入的财务值与条件估值不变；发现既有经济方法错误须独立解释和验证，不能包装成纯兼容清理。
- Go全套测试及CLI构建、Python相应回归通过；仅历史版本测试允许随明确取消的旧能力移除，当前安全和财务断言不得一并删掉。

## 首批验证

已执行 `gofmt`、存储及采集回归、`go test ./...` 和CLI构建，均通过；日线同运行重放、A→B→A观测历史、失败事务回滚及无运行ID写入拒绝保留。搜索接口默认行业表US的直接调用检查通过；本轮未调整WACC公式或经济政策。Python全套443项通过、4项既有外部样本缺失跳过；当前契约到条件估值的集成回归通过，旧转换器仍被历史测试使用，尚未满足最终无兼容验收。修改处本地链接与 `git diff --check` 通过。
