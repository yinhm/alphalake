# 固定规则的未来时间窗口

已保存2026H1起点、2027H1目标的120家公司预测台账。其中81家公司有当前规则、零增长及半量利润校准三组预测；39家因起点非正收入/EBIT不符合既有模型而拒绝，全部保留。目标实际、误差及有效性结论均为空。

[计划](forward-plan.json)由`0a96527`先提交，再生成[不可覆盖的预测归档](forward-2026H1.json.gz)。实际保存时间为2026-09-11T21:57:43.799552+00:00，不能称2026年6月时已预测；模型信息截止沿用2026-09-01中国零点。财务来自后来取得的TDX原始版本，不是严格PIT。公司仍是已用过的120家公司，时间窗口未来不等于新公司留出。

当前已经结束的是2026H1，目标TTM包含的2026Q3、2026Q4、2027Q1、2027Q2均尚未结束，当前不能评分。2025Q3→2026Q3则已有大部分构成季度可见，本轮未将它冒称全新的未来年度窗口。

## 固定规则

沿用[q3-protocol.json](q3-protocol.json)中的原60家公司训练、原120家公司评价名单、数值政策与半量规则，不改变样本或系数界限。预测日期使用该父协议已声明的H1截止规则。系数仅由2025H1→2026H1、截止2026-09-01中国零点可用的45个开发组合拟合，尺度0.5936120276515603，半量应用倍率0.7968060138257802；与既有当前期拟合一致。

三模型只预测一年收入与调整EBIT代理，单位百万元；不生成新的完整DCF、公司股价或现金流有效性结论。校准不改变收入，零增长只是对照。保留此前2025H1与Q3的失败，即使未来窗口通过，也只能增加证据，不能覆盖旧失败或直接扩大默认采用。

继续使用原验证门槛：至少80个可评价组合、利润主误差改善至少5%、收入WAPE非劣、收入中位误差最多退化5%、各期利润主误差最多退化5%、逐公司删除评价后的整体非劣。本次只是前置保存，没有任何门槛已经通过的声明。

## 到期后的工作

固定评价截止2027-09-01中国零点。届时须读取本次保存的预测、模型和拟合结果，不在新数据上重新生成过去预测；实际版本和FN314可用性单独归档。拒绝、缺期及评价失败仍留在120分母，不因81家可预测就改成81家总体。迟到或更正数据只能另建后续复核，不能覆盖第一次评价。

本轮交付是预测生成和保存，**尚未实现专用到期评分入口，也未配置定时任务**。下一项工程工作是让到期评分只读取冻结预测，并在目标未结束/固定截止未到时拒绝评分。等待未来数据是此时间窗口的客观条件，不代表整个估值准确度目标已经完成。

## 重放与验证

工具复用已有起点窗口、共享历史预测和过去窗口校准。输出使用排他创建，不覆盖已有文件；文件与输入、实现摘要见[回执](forward-receipt.json)。原快照和父协议均已在库，不下载新文件。

```bash
# 仅在目标期尚未结束时允许另行生成；新保存时间不同，不覆盖本次归档。
PYTHONPATH=valuation/backend python -m tools.prepare_temporal_forecasts \
 valuation/research/tdx-growth-expanded/forward-plan.json /tmp/alphalake-forward-replay.json.gz
# 重放提交归档中的保存时间与数据，适用于后续代码回归。
PYTHONPATH=valuation/backend python -m pytest valuation/backend/tests/test_temporal_forecasts.py -q
```

两项回归通过：120个位置完整重放，注入重复的未来零值记录不改变拟合或预测；保存时间跨过目标期会拒绝；另用既有2024H1成熟窗口，86个可评价公司的三组预测逐项相同。真实原文未在本轮重新审核，所有边界沿用简化TDX研究。已有Python依赖与pytest CI覆盖，零skip；Go全套测试及构建通过，无生产政策或主库变更。
