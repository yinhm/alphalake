# 固定五家公司三起点标准链盘点

已按`7fca097`冻结的[计划](plan.json)完成一次主库只读核查。15个公司/起点中1个可完成既有模型重放及到期核验，14个首先因收入TTM缺失阻断，其中9个没有标准事实。此结果衡量本库历史交付范围，不是TDX源数据覆盖率或预测准确率。

数据库为`workspace/auto-valuation-20260909/market.duckdb`，五家公司与既有研究一致；起点2023/2024/2025H1，预测信息截止为起点年9月1日中国零点，实际截止2026-09-10 UTC零点。数据版本后来取得，不是严格PIT。冻结政策的数值全部沿用历史模板，WACC10%、税率25%、收入/资本倍率3等不因本轮结果改变；除安克外不沿用安克业务范围审核。这里的通过仅指技术路径可运行，不构成新的公司业务审核。

| 公司 | 2023H1 | 2024H1 | 2025H1 |
| --- | --- | --- | --- |
| 安克 | 零标准事实 | 收入TTM缺期 | 模型重放及一年到期核验 |
| 苏泊尔 | 零标准事实 | 零标准事实 | 收入TTM缺期 |
| 格力 | 零标准事实 | 零标准事实 | 收入TTM缺期 |
| 伊利 | 零标准事实 | 零标准事实 | 收入TTM缺期 |
| 海天 | 零标准事实 | 零标准事实 | 收入TTM缺期 |

阻断原因是现有适配器遇到的第一个失败项，不声称补齐收入即可估值。格力/伊利金融兼营和海天历史版本边界仍保留；不因收入先报错就认定后续经营范围审核通过。快照中完整窗口及缺期列表均保留，未把无标准事实的证券从分母移除。

唯一成功位置沿用安克2025H1已有政策、数据口径及模型，仍是十年中1项到期、9项未到期，不是新独立样本。该例不能提供跨公司预测改善证据；收入、利润到期误差不代表FCFF或终值有效性。未下载新财报、未扩展字段映射、未更改主库或生产政策。

## 决策

简化回溯继续用独立TDX研究层，不要求先为全部历史起点补CNINFO。这次盘点明确两条交付边界：研究切片已有多起点评价，但生产标准历史链在这五家公司中仅此一项能够跑通。不可把研究评分数当作主库交付数，也不可为了出值用研究源切片静默替代标准事实。

本轮盘点到此停止，不继续逐公司追查历史公告。后续预测候选需有新的经济依据和冻结验证方案；不能为了把本表填满而偏离预测改进主线。此前停止的增长平滑、单研报选择、管理层目标等规则不借此次盘点重新调参。

## 重放与校验

[完整归档](result.json.gz)约84KB，含15份标准导出、唯一成功运行及其实际快照，无须本地数据库即可重放。工具复用`export-valuation`只读命令、既有适配器、估值保存、运行重放和到期核验，未新建预测引擎。

```bash
# 从主库重新盘点，输出目录必须不存在；之后的数据库变化可能改变结果。
PYTHONPATH=valuation/backend python -m tools.audit_standard_history_five \
 valuation/research/standard-history-five workspace/standard-history-five-replay
# 离线重放提交的这次数据版本，不要求主库存在。
PYTHONPATH=valuation/backend python -m pytest valuation/backend/tests/test_standard_history_five.py -q
```

回归逐项比较15位置、完整模型与到期结果，确认只有16次导出调用（15起点加1实际）；未来披露篡改后原唯一成功项拒绝、不查询实际，其余14项仍保留。使用项目现有Python依赖及pytest CI，无PDF条件skip。代码变更后旧归档若因引擎版本发生变化，需明确兼容重放，不将新运行元数据当作预测数值更新。

本轮本地验收：对应Python回归1项通过，无skip；`go test ./...`、`go build ./cmd/alphalake`和`git diff --check`通过，无依赖变更。
