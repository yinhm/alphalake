# 完整TTM同比的一年预测候选

**开发未通过，不采用、不进入新留出。** `129ef71`先固定[协议](protocol.json)，复用600家既有开发名单和源快照，在2023H1、2024H1、2025H1三个起点比较一年收入预测。信息截止为起点当年9月1日中国零点，目标为下一年同期TTM，实际评价截止沿用2026-09-10中国零点；属于后来版本的TDX简化回溯，不是严格PIT。

候选只将增长取值改为`当前TTM收入/上年同期TTM收入−1`，沿用原−10%/20%上下限、起点利润率和一年期限。原规则从上年1月1日起的可用季度输入生成同比中位数，复用生产共享函数；没有改变DCF或达摩达兰再投资/回报定义。完整年度窗口可能减少季度构成影响只是待检验假设，不预设它更准确。

[完整结果](development-result.json.gz)保留1,800组：原规则1,265组可评价，候选新增37组历史不足/不可用而缩至共同1,228组（498家公司），覆盖保留97.08%；535组原始阻断仍在。这里沿用通用经营预测的正收入/正EBIT要求，与先前不要求正利润的1,726组收入研究分母不同，不能横向拼接误差。金融兼营及历史可比性仍未获得逐公司审核，调整EBIT仍为合并代理，不代表生产准入。

| 共同样本指标 | 原规则 | 完整TTM同比 | 零增长 |
| --- | ---: | ---: | ---: |
| 收入绝对百分比误差均值 | 21.3384% | 22.2216% | 20.3412% |
| 收入WAPE | 14.1112% | 15.2719% | 13.9261% |
| EBIT代理MAE/实际收入 | 8.8100% | 8.9050% | 8.5600% |

| 起点 | 原规则可评价 | 共同可评价 | 原规则收入MAE | 候选收入MAE |
| --- | ---: | ---: | ---: | ---: |
| 2023H1 | 417 | 398 | 34.7831% | 35.3407% |
| 2024H1 | 426 | 413 | 14.7044% | 16.0558% |
| 2025H1 | 422 | 417 | 15.0767% | 15.8070% |

样本量与95%覆盖保留门槛通过，改善5%、WAPE、零增长对照、利润不退化及各起点容忍门槛未全部通过；三个起点收入误差都变差。五家公司先前表现不参与本候选调参；600家是既有开发样本，不称独立验证。到此停止，不扫描窗口、权重、上下限或逐疫情异常取证以补救。

## 调用与验证

实现复用`forecast_rows`的生产增长基线、源时点检查，以及既有`revenue`和误差函数。候选在目标实际缺失时仍保留起点可计算的预测，不让未来披露决定预测是否生成。旧研究代码与生产规则未改，无新增依赖、下载、数据库写入或源数值变化。

仓库根目录，使用已安装后端依赖的Python：

```bash
PYTHONPATH=valuation/backend workspace/anker-agent-adapter-20260906/venv/bin/python -m tools.backtest_tdx_ttm_yoy valuation/research/tdx-ttm-yoy/protocol.json > /tmp/alphalake-ttm-yoy-result.json
PYTHONPATH=valuation/backend workspace/anker-agent-adapter-20260906/venv/bin/python -m pytest valuation/backend/tests/test_tdx_ttm_yoy.py valuation/backend/tests/test_tdx_multiyear_growth.py -q
```

三项对应回归通过：完整真实结果重放、Decimal候选复利计算、目标期收入缺失不改变起点/预测、额外历史缺失/重复拒绝、源哈希篡改拒绝；新测试纳入现有pytest CI，无新增skip。Go全套测试与构建通过；本轮Python只运行新研究及其直接复用路径的回归，未重复整套后端测试，上一轮全套375项通过的记录不充作本轮全套验收。
