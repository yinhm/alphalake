# AlphaLake

- 使用中文沟通和编写文档。先读 `README.md`、`docs/implementation-status.md`，涉及语义时查 `docs/decisions/`；不要把设计中的计划当成已实现功能。当前状态与历史验收分开，更新功能时修正文档中的过时结论，不只在末尾追加。
- 先追踪调用链，再修根因；复用现有代码、Go 标准库和 DuckDB 能力，不为假想需求增加抽象或依赖。
- 工具实现与研究产物分开：独立 Go 工具入口放 `cmd/<工具名>/`，数据源解析及通用逻辑复用 `internal/`；Python 估值/研究工具放 `valuation/backend/tools/`。`valuation/research/` 只保留研究配置、证据快照、结果与说明，不混入工具实现；移动入口时同步命令和引用。
- 数据源语义必须有真实响应、原始文件或权威文档佐证，并将发现固化为回归测试；模拟测试通过不代表真实接口可用。特别检查空响应、分页尾页和未来报告期占位包。
- 不猜身份、单位或时间：按业务时点解析证券身份，歧义保留待解析证据；报告期不等于公告时间；交易披露、交割/登记与会计确认时点分别留痕，不互相替代。CNINFO 日期精度公告到中国次日零点才可用于 PIT。
- 财务数据分层不得绕过：TDX 是主要结构化数值来源，CNINFO 提供公告身份、时点和原文核验。原始证据→源解析→语义/校验→标准事实→财务派生→估值，来源独立留痕；PDF 不静默覆盖 TDX，冲突留诊断。
- 估值方法一律以达摩达兰原始教材及官方公开资料为准，方法或公式调整须注明出处和适用条件；增长、利润率等输入可依公司证据与历史复验校准，但不得为通过回测改写FCFF、净再投资、ROIC或WACC定义。行业均值及模型假设不冒充公司事实；既有方法偏差须记录并修正，不把本地实现当作方法依据。
- 源字段编号（如TDX FN）仅用于源解析、映射、校验及血缘；清洗物化后以通用财务字段和明确的单位、期间、报表范围交付。标准查询、派生计算、估值、准入及政策配置不得以FN取数或判断语义；不得只改展示名称。改造须保留原始证据，迁移旧库与输入契约，并验证既有财务数值及估值不变。
- 估值优先消费标准事实查询；确实缺失或尚未审核的字段必须分别列明，必要附注作为显式补充输入，政策/预测另层保存。不得用 PDF 直算样本冒充生产数据链路验收，完成度按系统实际交付的输入统计。
- 哈希、重复提取和恒等式各有边界，不等于独立语义核验。新增公司须验证 TDX→物化/时点查询→估值输入，并以财报核对期间、单位、范围及源精度；保留差异，不为对齐模型恢复 float32 丢失的小数或将缺项归零。
- 财务字段同时核对单位和期间：TDX FN230–FN237 是单季度金额，年报包中也不是全年累计；FN238 是期末股本。未审核字段只保留源证据。折旧等分量可能跨期合并/拆分列示，先按公司和期间核对组成范围，再聚合TTM；不固定叠加字段或将未单列认定为零。 历史比较区分原披露与追溯调整口径；定向发现差异保留诊断，不用最新PDF覆盖旧TDX，不把日期可用等同于数值可比。
- 保留不可变原始证据和血缘；失败或不完整快照不得覆盖可信历史、关闭身份区间或推进完成检查点。数据与对应检查点原子发布，重放保持幂等。
- 派生结果从本地标准事实重建，按内容变化判断失效；复权不得纳入存储价格锚点之前的事件。修正语义时同时考虑已有数据库的迁移或重算。
- 参考数据的发布日期、业务生效日、首次取得和本次取得分别留痕；未知日期不借用其他数据集日期，重复下载不改写首次取得。行业映射、资本效率、信用档与目标权重是显式政策代理，不冒充公司事实或公司市场 WACC。
- 完成度须标明证券分母、报告期、信息截止及数据库范围；核心字段齐全、模型准入、估值产出和逐公司原文核验分别计数。保留零事实、拒绝与失败公司，副本/分区批次不冒称主库/单次全市场验收。
- 报告估值须标明模型/政策版本、财务与信息时点、股本基期及可追溯运行；专项研究与通用批次分别展示。不同模型不串成价格更新，条件值不称当前目标价，政策过期或缺项不静默降级。
- 解释估值变化先比实际输入，再区分财务、预测、WACC与股权/股本调整；声称单因素变化须验证其余输入相同。库升级或副本发布先保留备份、比较受影响内容并重开验收，运行元数据变化不等于财务值变化。
- 预测规则改进须先建立当前规则基线，固定样本、信息截止、实际值版本与留出期间；后来取得的历史数据不冒称当时已留存，更正/当前参考不得泄漏入历史预测。公式复算、估值成功率和贴近股价不代替预测有效性，保留拒绝与缺数据分母。同公司多期不冒称独立样本，点估计通过与不确定性分开报告；重采样不消除版本、幸存者或宏观共同冲击偏差。 经用户授权的简化回溯可在独立研究层采用TDX源数值与FN314日期、按异常定向核验CNINFO；不得伪造标准事实或声称严格PIT。
- 估值改进以持续经营公司的通用能力为主线；疫情异常、重大重组等个案仅作边界诊断，新增取证须说明会改变哪项估值决策。不得无期限追查异常或按事后误差筛出“正常”公司；真实周期和亏损保留。每轮候选复用既有基线、明确停止条件，失败不继续扫描阈值；最终交付须解释经营预测如何影响现金流和条件估值，局部误差改善不冒称完整DCF准确度提升。
- 异常值先区分解析错误、口径不可比与真实极端经营结果；不删除原始证据，不以亏损、负季度收入或高研发费用率直接判错。训练过滤须只用起点可得信息、事前固定规则并报告移除数，完整评价分母保留；按事后误差挑公司剔除只作敏感性，不用过滤后的好看结果证明预测有效。
- 代码修改后运行 `gofmt`、相关回归测试、`go test ./...` 和 `go build ./cmd/alphalake`；依赖变更检查 `go mod tidy`。Python 估值或解析改动运行对应回归，明确解释器依赖、CI覆盖与本地skip；新增真实证据须有可重复的正向与篡改拒绝校验。仅文档修改检查链接与 `git diff --check`，无需跑 Go 测试。行为变化同步相关文档，如实说明验证范围及未通过项。
